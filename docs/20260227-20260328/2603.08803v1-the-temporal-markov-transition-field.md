---
title: The Temporal Markov Transition Field
title_zh: 时间马尔可夫迁移场
authors: Michael Leznik
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08803v1"
tags: ["query:diffts"]
score: 7.0
evidence: 使用马尔可夫转移将时间序列编码为二维图像
tldr: 本文针对传统MTF无法区分时间变化动态的问题，提出了时间马尔可夫转移场（TMTF），通过将时间序列划分为若干片段并为每段构建局部转移矩阵，生成可反映不同时期动态特征的二维编码，从而在时变分析与CNN特征提取任务中提高表现力与 interpretability。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.08803v1/fig-001.webp\", \"caption\": \"Figure 1: The 12-point example series. Quantile bands are shaded. The dashed vertical line marks the chunk boundary for K = 2.\", \"page\": 4, \"index\": 1, \"width\": 761, \"height\": 252}, {\"url\": \"assets/figures/arxiv/2603.08803v1/fig-002.webp\", \"caption\": \"Figure 2: The assembled 12× 12 global MTF image for the worked example (Q = 3). Rowindex colours and left-side brackets group the three sets of identical rows: red for state 1 (i ∈ {1, 4, 7, 8}), green for state 2 (i ∈ {3, 6, 9, 10}), and blue for state 3 (i ∈ {2, 5, 11, 12}). Within each group the rows are pixel-for-pixel identical regardless of whether they fall in the mean-reverting first half or the persistent second half of the series. No band structure is visible; the regime transition at t = 6 leaves no trace.\", \"page\": 5, \"index\": 2, \"width\": 779, \"height\": 575}, {\"url\": \"assets/figures/arxiv/2603.08803v1/fig-003.webp\", \"caption\": \"Figure 3: The assembled 12× 12 TMTF image for the worked example (K = 2, Q = 3). Rows i = 1–6 (top band, above the dashed line) are governed by W (1) and contain only binary values {0, 1}, producing a stark alternating texture reflecting the deterministic mean-reverting cycle of chunk 1. Rows i = 7–12 (bottom band) are governed by W (2) and contain values in {0, 0.5, 1}, producing a softer texture reflecting the persistent upward dynamics of chunk 2. A CNN filter traversing the image detects the texture change at the band boundary and associates it with the regime transition at t = 6.\", \"page\": 9, \"index\": 3, \"width\": 779, \"height\": 508}]"
motivation: 现有的马尔可夫转移场（MTF）假设时序平稳，无法有效表达动态过程的时变特性。
method: 将时间序列划分为K个连续段，为每段构建局部转移矩阵，并组合生成具有时间分段特征的T×T图像。
result: TMTF在图像中形成反映不同时间段动态特征的横向纹理带，能更准确地捕捉时变规律。
conclusion: TMTF保留了时间信息且对幅度不敏感，适合用于时序模式分析和机器学习模型输入。
---

## 摘要
马尔可夫迁移场（Markov Transition Field, MTF）由 Wang 和 Oates 于 2015 年提出，它通过将时间序列的每一对时间步映射为其分位数状态之间的转移概率（由单一的全局转移矩阵估计）来将时间序列编码为二维图像。当转移动态具有平稳性时，这种构建方式是高效的；但当过程随时间发生状态转变时，全局矩阵会在不同状态间进行平均，从而导致得到的图像丢失关于各个动力学状态在何时激活的信息。本文提出了时间马尔可夫迁移场（Temporal Markov Transition Field, TMTF），这一扩展方法将时间序列划分为 $K$ 个连续时间区段，为每个区段估计独立的局部转移矩阵，并通过组合得到的图像使得每一行反映其对应区段的局部动态，而非全局平均。得到的 $T \times T$ 图像包含 $K$ 个水平纹理带，每个带代表一个时间段的转移动态。本文给出了形式化定义，建立了该表示的关键结构性质，展示了一个完整的数值示例以具体说明其与全局 MTF 的差异，分析了由于时间分段带来的偏差–方差权衡，并从过程特性（如持续性、均值回复和趋势行为）角度讨论了局部转移矩阵的几何解释。TMTF 对幅值不敏感且保持顺序特性，使其适合作为卷积神经网络处理时间序列特征任务的输入通道。

## Abstract
The Markov Transition Field (MTF), introduced by Wang and Oates (2015), encodes a time series as a two-dimensional image by mapping each pair of time steps to the transition probability between their quantile states, estimated from a single global transition matrix. This construction is efficient when the transition dynamics are stationary, but produces a misleading representation when the process changes regime over time: the global matrix averages across regimes and the resulting image loses all information about \emph{when} each dynamical regime was active. In this paper we introduce the \emph{Temporal Markov Transition Field} (TMTF), an extension that partitions the series into $K$ contiguous temporal chunks, estimates a separate local transition matrix for each chunk, and assembles the image so that each row reflects the dynamics local to its chunk rather than the global average. The resulting $T \times T$ image has $K$ horizontal bands of distinct texture, each encoding the transition dynamics of one temporal segment. We develop the formal definition, establish the key structural properties of the representation, work through a complete numerical example that makes the distinction from the global MTF concrete, analyse the bias--variance trade-off introduced by temporal chunking, and discuss the geometric interpretation of the local transition matrices in terms of process properties such as persistence, mean reversion, and trending behaviour. The TMTF is amplitude-agnostic and order-preserving, making it suitable as an input channel for convolutional neural networks applied to time series characterisation tasks.