---
title: Generative diffusion models for spatiotemporal influenza forecasting
title_zh: 用于时空流感预测的生成扩散模型
authors: "Joseph Lemaitre, Justin Lessler"
date: 2026-04-27
pdf: "https://arxiv.org/pdf/2604.24913v1"
tags: ["query:diffts"]
score: 10.0
evidence: 用于时空流感预测的扩散模型
tldr: 本文提出基于扩散模型的流感预测系统Influpaint，将流感季节表示为时空图像并通过条件生成实现预测，结合监测与模拟数据训练，生成真实多样的流感传播轨迹，预测性能优于传统方法并展示出对时空结构的出色建模能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.24913v1/fig-001.webp\", \"caption\": \"Figure 3: Full-season forecasts. a. Each panel for the United States, California, New York, and Texas shows reported hospitalizations (black line) and reference forecast dates (colored dashed vertical line), followed by forecast quantiles from 512 conditional trajectories (colored bands/lines) representing the probabilistic forecast. 10 example trajectories are highlighted as thin colored lines. b. Same as panel a, but for the 2024–2025 influenza season.\", \"page\": 6, \"index\": 1, \"width\": 918, \"height\": 445}, {\"url\": \"assets/figures/arxiv/2604.24913v1/fig-002.webp\", \"caption\": \"Figure 7: Training losses during Influpaint calibration. Curves show the training loss over 3,000 epochs for the candidate model configurations evaluated during calibration. The model selected for downstream analyses, based on its combined ranking in relative and absolute WIS, is highlighted in black.\", \"page\": 21, \"index\": 2, \"width\": 914, \"height\": 454}, {\"url\": \"assets/figures/arxiv/2604.24913v1/fig-003.webp\", \"caption\": \"Figure 8: Relationship between training loss and forecasting performance. Final training loss is compared with relative WIS (a) and absolute WIS (b) across candidate model configurations. Each point represents one model variant, and the configuration selected for downstream analyses is highlighted in red.\", \"page\": 22, \"index\": 3, \"width\": 890, \"height\": 379}, {\"url\": \"assets/figures/arxiv/2604.24913v1/fig-004.webp\", \"caption\": \"Figure 9: a. Each panel shows submitted 4-week-ahead FluSight forecasts from UNC_IDD-InfluPaint for North Carolina, New York, Texas, and Florida at the same reference dates used in the reference figure (colored dashed vertical lines). Forecast uncertainty is summarized by the submitted quantiles (colored fan) and median (colored line). The solid black line shows the observed final values. For the same reference dates, the FluSight ensemble forecast is shown as a dotted line. b. Same as panel a, but for the 2024–2025 influenza season.\", \"page\": 22, \"index\": 4, \"width\": 918, \"height\": 460}, {\"url\": \"assets/figures/arxiv/2604.24913v1/fig-005.webp\", \"caption\": \"Figure 5: Methods behind Influpaint. A. Epidemic seasons are represented as images with time and space as axes, and pixel intensity corresponding to incident hospitalizations. B. Denoising Diffusion Probabilistic Models generate data by training a U-Net to restore an image that has been progressively corrupted by Gaussian noise. C. For forecasting, the ground truth and a mask (green: observed values to preserve) are provided. At each denoising step, we stitch the noised ground truth and the intermediate denoised sample to obtain the updated trajectory.\", \"page\": 12, \"index\": 5, \"width\": 918, \"height\": 450}, {\"url\": \"assets/figures/arxiv/2604.24913v1/fig-006.webp\", \"caption\": \"Figure 4: Robustness to mask design. Black curves show observed hospitalizations during the 2023–2024 season; colored fans and lines show Influpaint predictive quantiles and medians. Insets show the conditioning mask for each panel, where green values are observed and red values are hidden and reconstructed by Influpaint. The arrow in each inset indicates which state/row is shown in the graph. a.1–3. Half-subpopulation spatial mask for California (a.1), Florida (a.2), and Maryland (a.3). b. Leave-one-stateout mask for North Carolina. c. Leave-one-state-out mask for Illinois. d. Midseason gap mask for Maryland. e. Past-only conditioning mask for Florida. f. Checkerboard spatial-temporal mask for California.\", \"page\": 9, \"index\": 6, \"width\": 918, \"height\": 477}, {\"url\": \"assets/figures/arxiv/2604.24913v1/fig-007.webp\", \"caption\": \"Figure 6: Ablation effects on WIS. Each point shows the mean paired change in absolute WIS for a model variant relative to the baseline configuration. Positive values indicate improved forecasting performance relative to the baseline, whereas negative values indicate worse performance. The first group reports diffusion-model denoising schedules, comparing 500 or 200 diffusion steps with either a linear (l) or cosine (c) variance schedule. The second group compares U-Net architectures, with 124 denoting a three-scale architecture with channel multipliers (1, 2, 4) and 1224 a deeper four-scale architecture with channel multipliers (1, 2, 2, 4). The dataset group compares training sets denoted xxSyyM, where xx% of samples come from surveillance data and yy% from modeled trajectories. The transform group compares square-root scaling of incidence values (Sqrt) with linear scaling (Lins). The enrichment group evaluates additional training perturbations: No (none), Pois (Poisson resampling), PoisPadScaleSmall (Poisson resampling with narrow temporal padding and intensity scaling), and PoisPadScale (Poisson resampling with wide temporal padding and intensity scaling). The final group compares the three inpainting configurations described in the main text.\", \"page\": 20, \"index\": 7, \"width\": 594, \"height\": 454}, {\"url\": \"assets/figures/arxiv/2604.24913v1/fig-008.webp\", \"caption\": \"Figure 2: 4-week-ahead forecasts. a. Each panel shows 4-week-ahead forecasts for North Carolina, New York, Texas, and Florida at multiple reference dates (colored dashed vertical lines). Forecast uncertainty is summarized by quantiles (colored fan) and the median (colored line) from 512 conditional trajectories. The solid black line shows the observed final values. For the same reference dates, the FluSight ensemble forecast is shown as a dotted line. b. Same as panel a, but for the 2024–2025 influenza season.\", \"page\": 5, \"index\": 8, \"width\": 918, \"height\": 458}]"
motivation: 现有流行病预测方法难以准确刻画复杂、不确定的疫情动态。
method: 作者提出Influpaint，将去噪扩散概率模型应用于流感时空预测，并采用混合实测与模拟数据训练。
result: Influpaint生成了多样且真实的疫情轨迹，在回顾和实时评测中表现与领先集成方法相当甚至更优。
conclusion: 研究表明，扩散模型能有效捕捉流感传播的时空结构，为传染病概率预测提供新途径。
---

## 摘要
预测传染病发病率可以为公共卫生规划提供重要信息，但由于流行病动态复杂，这一任务十分困难。目前的机理模型和统计方法常常难以捕捉多峰不确定性或突发趋势。Influpaint 将去噪扩散概率模型（denoising diffusion probabilistic models）应用于流行病预测。通过将流感季节编码为空间-时间图像，其中像素强度表示发病率，Influpaint 从监测数据和模拟轨迹组成的混合数据集中学习疾病动态的丰富分布。预测被建模为一个条件生成（inpainting）任务，从部分观测中推断完整轨迹。我们展示了 Influpaint 能够生成真实且多样的流行病轨迹，并在回顾性评估中取得了与领先集成方法相当的预测精度。在 2023–2025 年美国 CDC FluSight 挑战的实时评估中，各季节的表现显著提升，在 2024–2025 年期间预测高度准确但略显过度自信。当训练数据中包含 30% 的监测数据和 70% 的模拟轨迹时，性能最佳。这些结果表明，扩散模型能够捕捉流感动态中的重要时空结构，为概率性传染病预测提供了一个灵活的框架。

## Abstract
Forecasting infectious disease incidence can provide important information to guide public health planning, yet is difficult because epidemic dynamics are complex. Current mechanistic and statistical approaches often struggle to capture multimodal uncertainty or emergent trends. Influpaint adapts denoising diffusion probabilistic models to epidemic forecasting. By encoding influenza seasons as spatiotemporal images in which pixel intensity represents incidence, Influpaint learns a rich distribution of disease dynamics from a hybrid dataset of surveillance and simulated trajectories. Forecasting is formulated as a conditional generation (inpainting) task from partial observations. We show that Influpaint generates realistic, diverse epidemic trajectories and achieves forecast accuracy that is competitive with leading ensemble methods in retrospective evaluation. In real-time evaluation during the 2023--2025 U.S. CDC FluSight challenges, performance improved substantially across seasons, with highly accurate but somewhat overconfident projections in 2024--2025. The best performance was achieved with a training dataset containing 30% surveillance and 70% simulated trajectories. These results show that diffusion models can capture important spatiotemporal structure in influenza dynamics and provide a flexible framework for probabilistic infectious disease forecasting.

---

## 论文详细总结（自动生成）

# 用于时空流感预测的生成扩散模型（Influpaint）论文总结

---

## 一、研究动机与背景

- **核心问题**：传染病（尤其是季节性流感）的实时与中期预测对公共卫生决策至关重要，但传统方法面临多峰动态、不确定性高、行为与环境影响复杂等挑战。  
- **现状困境**：  
  - **机理模型**虽然能融入流行病学知识，但依赖强假设、易出现不现实的动力学。  
  - **统计模型**在识别历史模式方面表现良好，但当系统发生突变或遇到新病原体时容易失效。  
  - 两者都难以充分表达**多模态不确定性**。  
- **研究目标**：探索现代生成式 AI ——特别是去噪扩散概率模型（DDPM）在建模高维流行病动态中的潜力，提出一种能生成“多样且真实”的时空流感预测框架。

---

## 二、方法论：Influpaint 框架与核心算法

### 1. 总体思想
- 将每个**流感季节**表示为一个**二维时空图像**：
  - 横轴表示时间（按周），纵轴表示地域（州或地区），
  - 像素强度对应该时空点的发病率/住院人数。
- 使用**DDPM（Denoising Diffusion Probabilistic Model）**学习该图像的分布，实现从噪声到真实疫情图像的生成。
- 将**预测任务**转化为图像“**补画（inpainting）**”：在部分观测的历史上条件生成未来流行轨迹。

### 2. 关键技术步骤
1. **编码阶段**：将流感时间序列转换为 52×51 的矩阵结构（对应美国50州+DC）。  
2. **扩散过程**：  
   - **前向过程**：添加高斯噪声，使原始图像逐步退化为白噪声；  
     - 公式：\( q(x_t|x_0) = \mathcal{N}(x_t; \sqrt{\bar{\alpha_t}}x_0, (1-\bar{\alpha_t})I) \)。  
   - **反向过程**：训练 U-Net 网络预测噪声并逐步去噪，恢复真实样本。  
3. **训练目标**：最小化噪声估计误差 \( L(\theta) = E_{x_0, \epsilon, t}[\|\epsilon - \epsilon_\theta(x_t, t)\|^2] \)。  
4. **条件生成（预测）**：采用改进的 **CoPaint 算法**  
   - 同时更新已观测和待预测区域的像素，避免边界断层问题；  
   - 使用优化的 O-DDIM 采样和“时间旅行”策略增强条件一致性。  
5. **网络结构**：多尺度卷积 U-Net + 残差模块 + 自注意力层 + 正弦时间步嵌入。

---

## 三、实验设计与数据来源

### 1. 数据集组成（混合训练集）
- **真实监测数据（20帧）**：
  - CDC FluView：流感样疾病百分比；
  - CDC FluSurv-NET：流感住院率（按州扩展）。  
- **模拟数据（约1240帧）**：
  - 来源于 Flu Scenario Modeling Hub 的多个轮次模拟；
  - 包含 flepiMoP 模型和多团队生成的情景轨迹。
- **混合比例实验**：
  - 100%实测、100%模拟、30%实测+70%模拟、70%实测+30%模拟四种组合。

### 2. 对比与评测基准（Benchmark）
- **主要对比方法**：美国 CDC 的 **FluSight 多模型集成预测**（现行最稳定的官方基准）。  
- **评估指标**：
  - Weighted Interval Score（WIS）：较低值表示预测更优；
  - 预测区间覆盖率（50%与90%）；
  - 平均绝对误差（MAE）。

### 3. 消融与对比实验
- 一系列 **单因素消融实验** 评估关键设计：
  - 扩散步数（200 vs 500）；
  - 噪声调度（线性 vs 余弦）；
  - 数据混合比例；
  - 数据增强策略（Poisson重采样、时序填充、强度缩放）；
  - 不同 U-Net 架构；
  - 不同 inpainting 时间调度。
- 共比较约数十种模型配置，覆盖两季实时挑战（2023–2024、2024–2025）。

---

## 四、资源与算力

- 使用 **Nvidia L40 和 A100 GPU** 进行训练与预测。  
- 批量大小：512；所需显存≥20GB。  
- 生成一个预测（512样本的轨迹集）耗时约 **20–40分钟**。  
- 训练时间未明确给出，但图示显示约 **3000个epoch** 的训练过程。  
- 文章指出计算开销“显著高于传统方法”，是当前可扩展性的一项限制。

---

## 五、实验数量与充分性评估

- **覆盖范围**：三季 FluSight 真实挑战 + 回顾性模拟评测 + 多种消融实验。  
- **数据来源多样**：结合真实与模拟轨迹，时空范围覆盖美国全部州。  
- **评测标准**：
  - 使用官方 WIS、MAE、覆盖率，与公共挑战数据集保持一致；
  - 多模型对比与重复实验确保统计可靠性。  
- **整体评价**：实验设计充分、指标客观且与国际标准接轨，尽管部分实时结果为“事后评测”而非严格实时。

---

## 六、主要结论与发现

- **性能**：
  - 在2023–2024季与2024–2025季，Influpaint 的总体预测精度与 FluSight 集成相当甚至更优；
  - 最佳配置（30%监测+70%模拟）在综合 WIS 上领先约9–12%。  
- **生成能力**：
  - 无条件生成出的流感季节图像表现逼真，多样性高；
  - 能再现单峰、双峰、多时点高发等现实模式。  
- **预测能力**：
  - 在短期（4周）预测中准确捕捉拐点；
  - 具有良好校准但略偏“过度自信”。  
- **方法验证**：
  - 证明扩散模型能够有效捕捉复杂的时空流感结构，展现出高维生成空间的预测潜力。

---

## 七、研究优点

- 将**时空疾病动态**转化为**图像生成任务**的创新性表示。  
- 利用**DDPM+CoPaint**实现条件采样，能处理任意时间与空间缺失（mask）。  
- 支持高度灵活的真实场景：
  - 可补全缺失地区、未来时间、时空断层等。  
- **混合训练策略**成功缓解了有限实测数据问题。  
- 与现实 FluSight 系统无缝对接，证明其**操作性与可落地性**。  
- 模型输出为**可采样分布**，自然表征预测不确定性。

---

## 八、不足与局限

- **实时性受限**：生成一组轨迹需较长计算时间，对实时部署不利。  
- **数据依赖问题**：
  - 模拟数据质量差异和偏差仍可能影响模型泛化；
  - 模拟-only训练效果反而劣于实测-only，说明合成数据仍存在结构偏差。  
- **未考虑报告延迟和回填问题**，限制了实际流行病监测场景的适用性。  
- **可解释性低**：属于纯深度生成模型，难以提供流行病学机理解释。  
- **验证时间短**：实测仅跨三季，尚不足以代表长期稳定性。  
- **算力消耗高**：GPU需求大，生成速度慢；尚无轻量化版本。

---

（完）
