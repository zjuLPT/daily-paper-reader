---
title: Self-Organizing Score-based Data Assimilation
title_zh: 自组织的基于得分的数据同化方法
authors: "Yuma Yamaoka, Seiichi Uchida, Shoji Toyota"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28048v1"
tags: ["query:diffts"]
score: 10.0
evidence: 利用扩散模型从观测时间序列数据中推断潜状态
tldr: 本文提出了一种自组织评分驱动数据同化方法，用于在非线性、高维状态空间模型中同时推断潜在状态和未知参数。该方法结合扩散模型的高效 SDA 框架与经典自组织技术，实现高效联合推断并在神经科学和大气科学等复杂动态系统中验证其性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.28048v1/fig-001.webp\", \"caption\": \"Fig. 1. Overview of the training and inference procedure in Score-based Data Assimilation (SDA) [1]. We first simulate samples from a given statespace model. These samples are used as a training dataset to train a diffusion model for sampling latent states x1, ...,xT conditioned on a given time-series observation y1, ...,yT .\", \"page\": 1, \"index\": 1, \"width\": 510, \"height\": 302}, {\"url\": \"assets/figures/arxiv/2603.28048v1/fig-002.webp\", \"caption\": \"Fig. 7. Visualization of the inferred latent states x1:T for window sizes w ∈ {3, 5, 7, 9}, with the ground-truth x (true)\", \"page\": 6, \"index\": 2, \"width\": 510, \"height\": 321}, {\"url\": \"assets/figures/arxiv/2603.28048v1/fig-003.webp\", \"caption\": \"Fig. 3. We simulate the latent states x1:T and the model parameter θ to train the diffusion model. Subsequently, we use the trained model to jointly infer the latent states and model parameter conditioned on the observations y1:T .\", \"page\": 3, \"index\": 3, \"width\": 518, \"height\": 248}, {\"url\": \"assets/figures/arxiv/2603.28048v1/fig-004.webp\", \"caption\": \"Fig. 4. In contrast to the naive method described in Fig. 3, we simulate the augmented latent states z1:T = (x1:T ,θ1:T ) to train the diffusion model. As a result, the inferred model parameter is obtained as a trajectory θ1:T rather than a static parameter θ.\", \"page\": 4, \"index\": 4, \"width\": 520, \"height\": 246}, {\"url\": \"assets/figures/arxiv/2603.28048v1/fig-005.webp\", \"caption\": \"Fig. 5. Evaluation metrics across window sizes w ∈ {3, 5, 7, 9, 17, 25, 33, 65}, compared with the particle filter (PF; red dashed line), for FHN (top row) and Lorenz-63 (bottom row). Panels show: (a) expected log-likelihood, (b) Wasserstein distance for the latent states, and (c) RMSE of the model parameter θ.\", \"page\": 5, \"index\": 5, \"width\": 524, \"height\": 323}]"
motivation: 现有 SDA 方法无法处理包含未知参数的状态转移问题。
method: 将经典自组织技术引入基于扩散模型的 SDA 框架以实现联合推断。
result: 数值实验在神经和大气系统中验证了方法的有效性，并展示了其在高维数据下的可扩展性。
conclusion: 新方法成功结合现代 SDA 与自组织技术，实现了参数与状态的高效联合推断。
---

## 摘要
状态空间模型是一种统计框架，用于从观测到的时间序列数据中推断潜在状态。然而，对于非线性和高维状态空间模型的推断仍然具有挑战性。为此，基于扩散模型——一种强大的深度生成模型类别——的得分驱动数据同化（Score-based Data Assimilation, SDA）方法被提出。然而，当潜在状态的转移依赖于必须与潜在状态一起推断的未知参数时，SDA无法直接应用。为克服这一限制，我们提出了一种框架，使SDA能够处理具有未知参数的潜在状态。该方法的一个关键特征是引入了自组织技术，该技术在传统状态空间建模中用于潜在状态和参数的联合估计。通过将这一传统技术整合到现代SDA中，我们的方法实现了潜在状态与未知参数的联合推断，同时保持了SDA的高训练效率。通过在神经科学和大气科学中的动力系统上的数值实验验证了该方法的有效性。此外，其可扩展性在一个高维Kolmogorov流中得到了展示，该数据维度约为数十万。

## Abstract
A state-space model is a statistical framework for inferring latent states from observed time-series data. However, inference with nonlinear and high-dimensional state-space models remains challenging. To this end, an approach based on diffusion models-a powerful class of deep generative models-has been developed, known as Score-based Data Assimilation (SDA). However, SDA cannot be directly applied when the latent-state transition depends on unknown parameters that must be inferred jointly with the latent states. To overcome this limitation, we propose a framework that enables SDA to handle latent states with unknown parameters. A key feature of the proposed method is the incorporation of the self-organization technique, which has been used in classical state-space modeling for the joint estimation of latent states and parameters. By integrating this classical technique into modern SDA, our method enables joint inference of latent states and unknown parameters while maintaining the high training efficiency of SDA. The effectiveness of the proposed approach is validated through numerical experiments on dynamical systems arising in neuroscience and atmospheric science. In addition, its scalability is demonstrated using a high-dimensional Kolmogorov flow, with the data dimension on the order of several hundred thousand.

---

## 论文详细总结（自动生成）

# 《自组织的基于得分的数据同化方法》论文总结

## 一、研究问题与整体动机

- **核心问题**：  
  状态空间模型（State-Space Model, SSM）是时间序列分析中用于推断潜在状态的一种基础框架。然而，当状态转移动态复杂、非线性且高维时，传统推断方法（如扩展卡尔曼滤波、集合卡尔曼滤波或粒子滤波）往往效率低且不稳定。  
  近年来，“基于得分的数据同化”（Score-based Data Assimilation, SDA）利用扩散模型的生成能力实现了高维状态估计，但SDA默认系统参数已知，无法处理**动态方程中包含未知参数**的场景。

- **研究动机**：  
  在现实的物理与工程问题（如神经系统建模、气候模拟、流体力学等）中，模型参数往往未知并需同时估计。缺乏同时推断状态与参数的能力会导致模型校准误差和物理解释能力不足。

- **研究目标**：  
  作者旨在扩展 SDA，使其能够**联合估计潜在状态与未知模型参数**，并保持 SDA 原有的高维可扩展性与高训练效率。

---

## 二、方法论：自组织的得分驱动数据同化框架（SO-SDA）

### 1. 核心思想
- 将传统SDA中的潜在状态 \(x_{1:T}\) 与系统参数 \(\theta\) 一同纳入推断目标，构建联合后验分布：
  \[
  p(x_{1:T}, \theta | y_{1:T}) = \frac{\prod_{t=1}^T p(y_t | x_t) p(x_{1:T}, \theta)}{p(y_{1:T})}
  \]
- 引入**自组织（Self-Organization）技术**，将静态参数\(\theta\)推广为“时间序列化”的变量\(\theta_{1:T}\)，使整体变量可被扩散模型视为**马尔可夫序列**，从而可以像SDA一样利用窗口化采样训练。

### 2. 技术细节
- **扩散模型训练**  
  模拟生成多个样本对\((x_{1:T}, \theta)\)，将其作为训练集学习去噪得分函数 \(\nabla_{x_a} \log p(x_a, \theta)\)。  
  推断阶段，通过反向扩散过程生成与观测一致的联合轨迹样本。
  
- **自组织式状态扩展**  
  定义扩展变量：
  \[
  z_t = [x_t, \theta_t]^\top
  \]
  其中\(\theta_t = \theta_{t-1} + \varepsilon_t,\ \varepsilon_t \sim \mathcal{N}(0, \Sigma)\)，以缓解参数恒定导致的非马尔可夫性问题。

- **窗口化训练机制**  
  类似原SDA的局部窗口机制，将长时间序列分解成长度为 \(w\) 的短片段，以降低模拟和训练的计算成本，兼顾收敛与精度。

- **最终参数估计**  
  对输出的\(\theta_{1:T}\)序列取其时间平均值作为最终参数估计。

---

## 三、实验设计

### 1. 实验系统与数据集
- **低维连续动力系统**
  - **FitzHugh–Nagumo（FHN）模型**：神经元放电动力系统，未知参数为 \(a\)。
  - **Lorenz-63 系统**：经典大气动力系统，未知参数为 \(\rho\)。
- **高维流体力学系统**
  - **Kolmogorov Flow**：二维湍流系统，未知参数为雷诺数 \(Re\)，潜在维度达 \(64×64×33 ≈ 1.35×10^5\)。

### 2. 实验设置
- 模拟生成训练数据集（每个系统几千至上万条轨迹）。
- 观测过程：仅观察部分状态分量并加入高斯噪声。
- 通过改变**窗口长度 \(w\)**（如 3, 5, 7, 9, 17, 25, 33, 65）评估窗宽对性能的影响。

### 3. 对比基线
- **粒子滤波（Particle Filter, PF）**：作为参考方法，用于低维系统的基准真值比较。
- 未与其他深度方法（如EnKF或变分滤波）直接对比。

### 4. 评价指标
- 期望似然值（Expected log-likelihood）  
- Wasserstein 距离 \(W_1\)  
- 参数估计均方误差（RMSE）  
- 动力一致性指标（Equation Loss, \(D_{equation}\)）

---

## 四、资源与算力

- 文中**未明确说明**所用硬件资源（如GPU型号、数量、训练时长）。  
  鉴于数据量（百万级参数量的Kolmogorov流）、采用深度扩散网络推断，可推测实验使用了**中高端GPU集群（例如 A100/RTX 类）**；但论文中无明确算力、时长或能耗统计，属潜在缺失信息。

---

## 五、实验数量与充分性

- 共设计**三组系统实验**（FHN、Lorenz-63、Kolmogorov Flow），每组包含多个窗口长度对比。
- 对低维系统使用PF作为参考后验，验证推断的客观性与精度。
- 结果覆盖从**中低维非线性系统到高维湍流系统**，具有较强代表性。
- 然而，论文**未包含消融实验**（如对自组织模块的去除、不同噪声水平等），也未呈现多噪声条件或真实观测数据验证，实验仍偏重模拟验证阶段。

---

## 六、主要结论与发现

- **SDA成功扩展到联合参数估计场景。**  
  将自组织思想融入SDA后，实现了对潜在状态和未知参数的联合高效采样。
- **性能验证有效：**
  - FHN和Lorenz-63系统中，当窗口 \(w ≥ 17–25\)，结果接近粒子滤波的最优性能；
  - Kolmogorov流案例中，窗口 \(w=5\) 取得最低状态RMSE(≈0.052)与参数RMSE(≈0.107)。
- **方法可扩展到高维系统**，在约13万维数据上仍能保持稳定推断。

---

## 七、优点与亮点

- ✅ **创新融合**：首次将经典“自组织参数估计”技术融入现代扩散模型推断体系。  
- ✅ **高维可扩展性**：通过窗口化分解保持计算可控，同时支持上十万维状态。  
- ✅ **无分布假设**：不依赖线性、高斯假设，适用于复杂非线性系统。  
- ✅ **联合推断能力**：同时重构潜在轨迹与系统参数，可进行物理模型校准与不确定性量化。  
- ✅ **验证场景多样**：横跨神经科学、气象、流体动力等多学科系统。

---

## 八、不足与局限

- ⚠️ **缺乏算力与效率指标**：未报告训练耗时、计算复杂度，对方法实际可行性评估有限。  
- ⚠️ **数据依赖于仿真环境**：实验均为合成数据，缺乏真实观测验证。  
- ⚠️ **对比方法单一**：仅与粒子滤波(PF)比较，缺乏与现代基于深度学习的滤波或同化方法（如 Neural SSM、EnKF-Net）的横向性能对比。  
- ⚠️ **缺少消融研究**：未独立分析自组织模块或窗口参数的作用机理。  
- ⚠️ **参数更新噪声假设简单**：\(\theta_t\)仅采用固定协方差的随机游走模型，可能无法充分表达实际系统参数动态。

---

**（完）**
