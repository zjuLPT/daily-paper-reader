---
title: Self-Organizing Score-based Data Assimilation
title_zh: 自组织的基于得分的数据同化方法
authors: "Yuma Yamaoka, Seiichi Uchida, Shoji Toyota"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28048v2"
tags: ["query:diffts"]
score: 9.0
evidence: 基于评分的扩散模型用于时间序列隐状态推断
tldr: 本文提出了一种自组织得分驱动数据同化（Self-Organizing SDA）框架，将传统状态空间模型的自组织技术与现代得分基数据同化方法结合，实现了同时推断潜在状态和未知参数的能力，并在多个高维复杂系统中验证了其高效性和优越性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.28048v2/fig-001.webp\", \"caption\": \"Fig. 1. Overview of the training and inference procedure in Score-based Data Assimilation (SDA) [1]. We first simulate samples from a given statespace model. These samples are used as a training dataset to train a diffusion model for sampling latent states x1, ...,xT conditioned on a given time-series observation y1, ...,yT .\", \"page\": 1, \"index\": 1, \"width\": 510, \"height\": 302}, {\"url\": \"assets/figures/arxiv/2603.28048v2/fig-002.webp\", \"caption\": \"Fig. 7. Visualization of the inferred latent states x1:T for window sizes w ∈ {3, 5, 7, 9}, with the ground-truth x (true)\", \"page\": 6, \"index\": 2, \"width\": 510, \"height\": 321}, {\"url\": \"assets/figures/arxiv/2603.28048v2/fig-003.webp\", \"caption\": \"Fig. 3. We simulate the latent states x1:T and the model parameter θ to train the diffusion model. Subsequently, we use the trained model to jointly infer the latent states and model parameter conditioned on the observations y1:T .\", \"page\": 3, \"index\": 3, \"width\": 518, \"height\": 248}, {\"url\": \"assets/figures/arxiv/2603.28048v2/fig-004.webp\", \"caption\": \"Fig. 4. In contrast to the naive method described in Fig. 3, we simulate the augmented latent states z1:T = (x1:T ,θ1:T ) to train the diffusion model. As a result, the inferred model parameter is obtained as a trajectory θ1:T rather than a static parameter θ.\", \"page\": 4, \"index\": 4, \"width\": 520, \"height\": 246}, {\"url\": \"assets/figures/arxiv/2603.28048v2/fig-005.webp\", \"caption\": \"Fig. 5. Evaluation metrics across window sizes w ∈ {3, 5, 7, 9, 17, 25, 33, 65}, compared with the particle filter (PF; red dashed line), for FHN (top row) and Lorenz-63 (bottom row). Panels show: (a) expected log-likelihood, (b) Wasserstein distance for the latent states, and (c) RMSE of the model parameter θ.\", \"page\": 5, \"index\": 5, \"width\": 524, \"height\": 323}]"
motivation: 传统SDA无法处理中包含未知参数的状态空间模型，需要扩展以实现参数与状态的联合推断。
method: 方法将自组织技术引入得分基数据同化，实现潜在状态与未知参数的联合估计。
result: 通过数值实验验证方法在神经科学和大气科学的动力系统上效果优异，并在高维Kolmogorov流上展示出良好可扩展性。
conclusion: 自组织SDA兼具高效训练和参数-状态联合推断能力，为高维非线性动态系统的数据同化提供了新方案。
---

## 摘要
状态空间模型是一种用于从观测到的时间序列数据中推断潜在状态的统计框架。然而，对于非线性和高维状态空间模型的推断仍然具有挑战性。为此，基于扩散模型（这是一类强大的深度生成模型）的方法——基于得分的数据同化（Score-based Data Assimilation, SDA）——被提出。然而，当潜在状态的转移依赖于必须与潜在状态联合推断的未知参数时，SDA无法直接应用。为克服这一局限，我们提出了一种使SDA能够处理具有未知参数的潜在状态的框架。该方法的一个关键特征是引入了自组织技术，这一技术在经典状态空间建模中用于潜在状态与参数的联合估计。通过将这一经典技术结合到现代SDA中，我们的方法在保持SDA高训练效率的同时，实现了潜在状态与未知参数的联合推断。通过对神经科学和大气科学中的动力系统的数值实验验证了该方法的有效性。此外，利用维度达到数十万的高维Kolmogorov流数据展示了其可扩展性。

## Abstract
A state-space model is a statistical framework for inferring latent states from observed time-series data. However, inference with nonlinear and high-dimensional state-space models remains challenging. To this end, an approach based on diffusion models-a powerful class of deep generative models-has been developed, known as Score-based Data Assimilation (SDA). However, SDA cannot be directly applied when the latent-state transition depends on unknown parameters that must be inferred jointly with the latent states. To overcome this limitation, we propose a framework that enables SDA to handle latent states with unknown parameters. A key feature of the proposed method is the incorporation of the self-organization technique, which has been used in classical state-space modeling for the joint estimation of latent states and parameters. By integrating this classical technique into modern SDA, our method enables joint inference of latent states and unknown parameters while maintaining the high training efficiency of SDA. The effectiveness of the proposed approach is validated through numerical experiments on dynamical systems arising in neuroscience and atmospheric science. In addition, its scalability is demonstrated using a high-dimensional Kolmogorov flow, with the data dimension on the order of several hundred thousand.

---

## 论文详细总结（自动生成）

# 自组织的基于得分的数据同化方法（Self-Organizing Score-based Data Assimilation）论文总结

---

## 一、研究背景与核心问题

- **问题背景**：  
  状态空间模型（State-Space Model, SSM）是一种强有力的时序建模框架，用于从噪声观测中估计系统的隐状态。然而，在处理**非线性、高维动态系统**时，传统方法如扩展卡尔曼滤波（EKF）、集合卡尔曼滤波（EnKF）和粒子滤波（PF）面临效率和精度的瓶颈。  
- **已有研究局限**：  
  基于扩散模型的“**得分驱动数据同化（Score-based Data Assimilation, SDA）**”已成功将深度生成模型引入状态估计，在高维系统中表现出色，但它假设动态方程的参数是**已知固定的**。  
  实际问题（如气象建模、神经系统建模）中的许多系统包含**未知的动力学参数**，这些参数需要与隐状态**联合推断**。  
- **研究动机**：  
  本文旨在扩展 SDA，使其能够同时估计**潜在状态与未知模型参数**，以实现更灵活、更通用的高维非线性系统同化框架。

---

## 二、核心方法与关键技术

### 1. 方法总体思想

- 提出一种新的框架称为 **自组织的基于得分的数据同化（Self-Organizing SDA, SO-SDA）**。
- 基本思路：将传统SDA与**自组织（Self-Organization）技术**相结合，使得SDA能够在联合空间中对**状态与参数**进行建模、采样和推断。
- 这种结合既保留了SDA的高训练效率，又能解决未知参数建模问题。

### 2. 技术细节

1. **状态空间设定**  
   系统动态由分布  
   \[
   p(x_{t+1} | x_t, \theta)
   \]
   描述，其中 \(\theta\) 为未知参数。目标是推断联合后验：
   \[
   p(x_{1:T}, \theta | y_{1:T})
   \]
   在高维观测 \(y_{1:T}\) 下。

2. **模型训练机制**  
   - 通过模拟生成训练样本 \((x_{1:T}, \theta)\)。  
   - 使用扩散模型（Diffusion Model）在联合空间上估计得分函数 \(\nabla_x \log p(x)\)，从而实现反向扩散采样。  
   - 由于观测模型假定高斯分布 \(p(y_t | x_t) = \mathcal{N}(A(x_t), \Sigma)\)，训练时可仅依赖无条件得分函数（无需显式观测建模）。

3. **引入自组织机制**  
   - 将参数 \(\theta\) 扩展为时间相关序列 \(\theta_t\)，构建**增强的隐变量序列**：
     \[
     z_t = [x_t, \theta_t]^T
     \]
   - 假设 \(\theta_t\) 服从随机游走：
     \[
     \theta_{t+1} = \theta_t + \epsilon_t, \quad \epsilon_t \sim \mathcal{N}(0, \Sigma)
     \]
   - 因此生成目标变为 Markov 序列：
     \[
     z_{t-1} \to z_t \to z_{t+1}
     \]
     允许像原 SDA 一样应用分段窗口因子化策略（window-based factorization）。
   - 最终计算得到的参数估计为 \(\theta_{1:T}\) 的均值。

4. **效率优化：窗口化训练**  
   - 借助 Markov 性，将长时间序列分解为短片段（窗口长度 \(w\)），显著降低模拟和训练成本，同时保持建模精度。

---

## 三、实验设计

### 1. 数据与模型设定

| 系统 | 类型 | 未知参数 | 维度 | 应用领域 |
|------|------|-----------|--------|------|
| FitzHugh–Nagumo 模型 (FHN) | 常微分方程 | a | 2 | 神经科学 |
| Lorenz–63 系统 | 混沌动力系统 | ρ | 3 | 大气科学 |
| Kolmogorov 流（Navier–Stokes） | 偏微分方程流体系统 | Re (雷诺数) | 135,168 | 流体动力学 |

### 2. 观测与噪声设定

- 仅观测部分状态分量（FHN取u，Lorenz取x；Kolmogorov取8×8下采样网格）
- 添加高斯噪声（典型方差：0.05² 或 0.1²）

### 3. 对比方法

- **基线算法**：粒子滤波（Particle Filter, PF）
  - 由于低维模型可得近似真实后验，用作参考 benchmark。  
- **窗口长度消融**：  
  不同窗口大小 \(w \in \{3, 5, 7, 9, 17, 25, 33, 65\}\)，用于平衡训练代价与推断精度。

### 4. 性能指标

- **Expected Log-Likelihood**（观测后验似然，越大越好）  
- **Wasserstein距 (W1)**（分布一致性，越小越好）  
- **RMSE**（状态与参数的均方根误差）  
- **Equation Loss \(D_{\text{equation}}\)**：动态一致性评估（Kolmogorov流场）。

---

## 四、资源与算力

- 原文未明确说明使用何种 GPU 或集群资源，也未报告训练时长、算力或参数规模。  
- 从模型尺寸及任务复杂度（Kolmogorov流达十万维）推测，作者可能使用多GPU或分布式训练，但细节未披露。  
- 推断与训练均依赖 JAX 框架及 jax-cfd 库（GPU加速的流体求解器）。

---

## 五、实验数量与充分性

- **总体实验数量**：
  - 三个系统场景 × 多窗口消融（最多八种窗口长度） ≈ 数十组完整实验。  
- **验证充分性**：
  - 包含低维（FHN、Lorenz）到超高维（Kolmogorov流）的全面覆盖。
  - 含参数估计可视化、性能指标对比、窗口敏感性分析。
- **公平性**：
  - 使用 PF 作为标准对照，同时控制观测噪声与系统设定一致。  
  - 未报告多次独立运行标准差除 Kolmogorov流表格外。

结论：实验设计较为全面，验证在不同复杂度系统上均成立，但计算资源细节略欠透明。

---

## 六、主要结论与发现

- **SO-SDA 实现了状态与参数的联合推断**，克服了原SDA无法处理未知参数的限制。  
- **性能接近最优的粒子滤波（PF）**：
  - 在 FHN 模型中，当窗口 \(w ≥ 25\) 时达到最佳；
  - 在 Lorenz-63 中，\(w ≥ 17\) 即可接近 PF 结果。
- **可扩展性**：
  - 在高维 Kolmogorov 流体任务（约13万维）中，SO-SDA 成功还原物理场与 Re 参数，展示了优良的高维可扩展性。
- **窗口长度影响**：
  - 小窗口（例如 w=5）往往在高维任务中表现更好；过大窗口易致高维难优化问题。

---

## 七、优点与创新点

- **方法创新**：
  - 将“自组织（Self-Organization）”这一经典状态空间建模思想引入深度得分生成模型。
- **理论统一性强**：
  - 兼容现代扩散模型训练机制与古典 SSM 推断框架。
- **高计算效率**：
  - 通过窗口化补偿高维模拟开销，实现大幅降本。
- **通用性强**：
  - 适用于非线性、非高斯、未知参数系统，且不依赖显式过渡模型。
- **可扩展到超高维系统**（超过10⁵维）。

---

## 八、不足与局限

- **算力与训练细节未披露**：无法复现实验效率或硬件开销。
- **模型参数估计依赖窗口选择**：过大会造成高维退化，过小则易信息不足。
- **未与更现代的神经滤波或变分推断方法对比**，仅与PF比较。
- **物理一致性未完美评估**：尽管引入方程误差指标，但未深入分析物理守恒性。
- **参数随机游走假设（θ_t平稳）**在更复杂系统中可能欠合理。

---

（完）
