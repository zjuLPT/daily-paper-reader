Title: Metropolis-Adjusted Diffusion Models

URL Source: https://arxiv.org/pdf/2605.09654v1

Published Time: Tue, 12 May 2026 02:10:25 GMT

Number of Pages: 25

Markdown Content:
# Metropolis-Adjusted Diffusion Models 

## Kevin H. Lam 1,∗ Tyler Farghly 2,∗ Christopher Williams 1,∗

## Jun Yang 3 Yee Whye Teh 1 Arnaud Doucet 11Department of Statistics, University of Oxford 

> 2

## Inria, CNRS, École Normale Supérieure, PSL Research University 

> 3

## Department of Mathematical Sciences, University of Copenhagen 

{lam,williams,y.w.teh,doucet }@stats.ox.ac.uk ,

tyler.farghly@inria.fr , jy@math.ku.dk 

Abstract 

Sampling from score-based diffusion models incurs bias due to both time discretisation and the approximation of the score function. A common strategy for reducing this bias is to apply corrector steps based on the unadjusted Langevin algorithm (ULA) at each noise level within a predictor-corrector framework. However, ULA is itself a biased sampler, as it discretises a continuous diffusion process. In this work, we consider adjusted Langevin correctors that employ Metropolis–Hastings (MH) or Barker’s accept-reject steps to correct for this bias. Since the target density ratio typically required by MH-based algorithms is unavailable, we propose methods that instead utilise the score function to compute the correct acceptance probability. We introduce the first exact method for adjusting Langevin corrections in diffusion models, based on a two-coin Bernoulli factory algorithm. We also propose an efficient approximation based on Simpson’s rule that achieves accuracy of order 5/2 in the step size at near-zero marginal cost. We demonstrate that these procedures improve sample quality on both synthetic and image datasets, yielding consistent gains in Fréchet Inception Distance (FID) on the latter. 

# 1 Introduction 

Diffusion models have become a key class of generative models, with state-of-the-art performance in domains such as image, audio, and protein design. They rely on a simple but powerful idea. Instead of modelling the data density directly, one learns its score along a noising path and then simulates a reverse-time diffusion process to transform noise into data (Sohl-Dickstein et al., 2015; Song and Ermon, 2019; Ho et al., 2020; Song et al., 2021). In practice, this reverse diffusion is implemented using numerical integrators (Song et al., 2021; Karras et al., 2022). Consequently, even if the learned score were exact, the generated distribution would still be impacted by time-discretisation error. With a learned score, this error is compounded by approximation error in the score network. 

> ∗Authors contributed equally to this work.

1

> arXiv:2605.09654v1 [stat.ML] 10 May 2026

Figure 1: DDM sampling using a probability flow ODE predictor, ULA correction, and MADM correction. Mean distance to the true samples and the 95% sample containment distance are shown. A coarse diffusion schedule and large ULA stepsize are chosen to show the discretisation bias, the same schedule and stepsize are used in MADM to show the bias correction from this method. This time-discretisation issue is common in Markov chain Monte Carlo (MCMC). To sample from a target distribution, the unadjusted Langevin algorithm (ULA), a discretisation of the Langevin diffusion, uses gradients of a log-density to explore the state space (Durmus and Moulines, 2017), but this does not leave the target distribution invariant. The standard approach to correct for this discretisation bias is to add an accept–reject step, yielding the Metropolis-adjusted Langevin algorithm (MALA) (Besag, 1994; Roberts and Tweedie, 1996). The analogy with diffusion sampling is immediate. Predictor–Corrector samplers (Song et al., 2021) already use Langevin-type corrector steps at fixed noise levels: after a predictor step moves the sample between successive marginals, a corrector step refines the sample with respect to the current marginal. The usual corrector is again ULA. Thus, the “corrector” in the Predictor–Corrector scheme is itself biased. It may reduce the error introduced by the predictor, but it introduces its own discretisation error. This motivates a natural question: can we replace ULA correctors in diffusion models by Metropolis-adjusted correctors? A direct application of MALA is not possible as it requires evaluating the density ratio pt(ex)/p t(x) between the proposal ex and the current state x for the intractable target marginal pt at noise level t, whereas diffusion models only provide a score estimate 

sϕ(x, t ) ≈ ∇ x log pt(x).To address this problem, we propose Metropolis-Adjusted Diffusion Models (MADM), a simple modification of the Predictor–Corrector framework where the usual ULA corrector is replaced by an accept–reject corrected Langevin step. The key point is that, although the density ratio is unavailable, its logarithm can be represented as a line integral of the score. This result shows that the score can be used not only to construct Langevin proposals, but also to estimate the acceptance probability that corrects their discretisation bias. We develop two approaches to exploit this representation. First, we give an exact Barker adjusted corrector based on a two-coin Bernoulli factory (Gonçalves et al., 2017). Under the assumption that s(·, t ) = ∇ log pt(·) and that a suitable bound on the estimator of the line integral is available, this procedure produces an accept–reject mechanism with the Barker acceptance probability without ever evaluating pt. This gives, to our knowledge, the first exact Metropolis-style Langevin corrector for any smooth target distribution only known through its score. Second, we introduce a practical approximation of the density ratio based on Simpson’s rule. The resulting MH correction is no longer exact, but it is very cheap. The resulting approximation has high-order accuracy in the Langevin step size, is easy to implement, and only requires access to a pre-trained score function. 2Empirically, Metropolis adjustments address a failure mode of unadjusted correctors on toy examples; see Figure 1. On image datasets, we combined a predictor with an MADM corrector based on Simpson’s rule using pre-trained models from Karras et al. (2022). While gains are modest in some unconditional settings, we found that MADM yields consistent and systematic improvements in FID. Our contributions are as follows: 

• We introduce MADM, a Metropolis-adjusted corrector framework for diffusion models that replaces biased ULA corrector steps. 

• We propose an exact method based on the Barker acceptance rule and a two-coin Bernoulli factory, giving an adjusted corrector under assumptions. 

• We present a scalable Simpson-rule approximation that requires only one additional midpoint score evaluation per proposal. 

• We demonstrate that MADM correctors reduce artifacts on synthetic datasets and consistently improve FID on standard image benchmarks. 

# 2 Background and Setup 

## 2.1 Langevin Diffusion and Unadjusted Langevin Algorithm 

Consider a smooth distribution p, then a standard technique for sampling from p is to simulate the Langevin diffusion 

dXt = g2 

> 2

∇x log p(Xt) d t + g dBt, (1) for some parameters g > 0. Whatever being the initial state X0, this diffusion is such that the law of Xt converges to p as t grows without bound. One cannot simulate exactly this nonlinear continuous-time diffusion process so a standard approach is to use an Euler–Maruyama integrator 

Xkh = X(k−1) h + hg2 

> 2

∇x log p(X(k−1) h) + √hg Zk, Zk ∼ N (0, I), (2) where h > 0 is a step-size. This is known as the Unadjusted Langevin Algorithm (ULA). This time-discretisation introduces a bias so that we do not sample exactly from p.

## 2.2 Diffusion Models 

A Denoising Diffusion Model (DDM) (Sohl-Dickstein et al., 2015; Ho et al., 2020; Song et al., 2021) uses a forward SDE to transport the data distribution pdata to a multivariate normal N (0, I)

through 

dXt = ftXt dt + gt dBt, X0 ∼ pdata , (3) where t ∈ [0 , 1] and Bt is a multivariate standard Brownian motion. The functions ft and gt must ensure that X1 ∼ N (0, I), at least approximately, but are otherwise design choices which can be selected to optimise sample quality; see, e.g. (Williams et al., 2024). The reverse-time process 

(Yt)t∈[0 ,1] = ( X1−t)t∈[0 ,1] thus transports N (0, I) to pdata and satisfies 

dYt = [ −f1−tYt + g21−t∇x log p1−t(Yt)] d t + g1−t dBt, Y0 ∼ N (0, I), (4) 3where pt is the distribution of Xt. In practice, the score function ∇x log pt(·) is unknown and is approximated by training a neural network sϕ(xt, t ) minimising the denoising score-matching loss (Vincent, 2011; Song and Ermon, 2019) 

Et∼U [0 ,1] ,X0∼pdata EXt|X0∼pt|0(·| X0)[wt∥sϕ(Xt, t ) − ∇ x log pt|0(Xt|X0)∥22] (5) where wt > 0 is a weight function and the conditional distribution is given by 

pt|0(xt|x0) = N (rtx0, r 2 

> t

σ2 

> t

I), rt = exp 

Z t

> 0

fu du



, σt =

sZ t

> 0

gu

ru

2

du. 

Different choices of ft and gt in Equation 3 yield the variance exploding (VE) and variance preserving (VP) SDEs, with rt and σt known in closed form (see Table 1 of Karras et al. (2022)). For convenience, we have assumed the VP setting (i.e. p1 = N (0, I)). Given ft and gt, together with a learned score and schedule, the task is to evolve Equation 4 to draw samples from the generative model. 

## 2.3 Diffusion Predictor-Corrector Framework 

Equation 4 can be split into the sum of a deterministic drift and a Langevin diffusion, in which the deterministic component defines the predictor step and the stochastic component the corrector: 

dYt =



−f1−tYt + g21−t 

> 2

∇x log p1−t(Yt)



dt

| {z }

> Probability Flow Prediction ODE

+

g21−t 

> 2

∇x log p1−t(Yt) d t + g1−t dBt

| {z }

> Langevin Correction SDE

.

This decomposition gives rise to the predictor–corrector framework where at each step we integrate numerically the ODE before applying the Langevin Correction SDE as described in Song et al. (2021). The rationale is that, if Yt−η ∼ p1−(t−η) then by integrating exactly the deterministic drift with an exact score over an interval of length η then we obtain Y′ 

> t

∼ p1−t and the Langevin diffusion initialised at such state leaves this distribution invariant and so provide a new sample 

Yt ∼ p1−t. Despite the terminology, the Langevin “corrector” is itself defined by an SDE and must be discretised as discussed in Section 2.1. Using an Euler–Maruyama scheme, this reads 

Y′ 

> kη

= Y(k−1) η + η



− f1−kη Y(k−1) η + g21−kη  

> 2

∇x log p1−kη (Y(k−1) η)



, (6) 

Ykη = Y′ 

> kη

+ g21−kη  

> 2

h∇x log p1−kη (Y′

> kη

) + √hg 1−kη Z, Z ∼ N (0, I), (7) where equation 7 is a single ULA step but we can use multiple steps. Equation equation 7 depends on only g, whereas Equation equation 6 depends on f and g, granting that upon discretisation, different step sizes for either predictor or corrector may be used with this ansatz. In summary, despite the terminology, the Langevin “corrector” is approximated by ULA as discussed in Section 2.1, so it does not yield exact samples from p1−t. While it may reduce errors introduced by the predictor, it introduces its own discretisation bias. In (Song et al., 2021), the step size is chosen adaptively, scaling inversely with the squared norm of the score, which in the context of ULA introduces additional bias. In the MCMC literature, explicit mechanisms exist to correct such bias and ensure exactness which we describe in the next section. 4Algorithm 1 MALA for Denoising Diffusion Models 

Input: initial sample x(0)  

> t

, score s(·, t ), corrector steps K, step size h.

for i = 0 to K − 1 do 

Compute ex(i+1)  

> t

= x(i) 

> t

+ h 

> 2

s(x(i) 

> t

, t ) + √hz(i) with z(i) ∼ N (0, I).Compute ACCEPT/REJECT using Algorithm 2 or 3 with state xt and proposal ex(i+1) 

> t

Set x(i+1)  

> t

= ex(i+1)  

> t

if ACCEPT; otherwise x(i+1)  

> t

= x(i) 

> t

.

end for return x(K) 

> t

.

## 2.4 Markov chain Monte Carlo 

We are interested in sampling from a target density p on Rd, known up to a normalising constant. In Section 2.1, we introduced ULA to address this problem but, even if one were to run ULA for millions of iterations, the Euler discretization introduces a non-vanishing bias. We describe here a generic MCMC algorithm which can be used to correct such bias. It generates a Markov chain 

(X(i))i≥1 whose equilibrium distribution is p.Given the current state X(i) = x of the Markov chain, we sample a candidate state ex from a proposal density q(ex | x). This candidate is accepted with probability αMH (x, ex) for the MH algorithm and αB(x, ex) for the Barker algorithm, where 

αMH (x, ex) = 1 ∧ R(x, ex), αB(x, ex) = R(x, ex) (1 + R(x, ex)) −1, (8) with 

R(x, ex) = H(x, ex)rp(x, ex), H(x, ex) = q(x | ex)

q(ex | x), rp(x, ex) = p(ex)

p(x). (9) If the proposal is accepted then we set X(i+1) = ex, otherwise X(i+1) = X(i). The popularity of the MH acceptance function stems from the result of Peskun (1973), which shows that, among reversible accept–reject rules, it minimises the asymptotic variance of ergodic averages. However, Łatuszyński and Roberts (2013) demonstrated that Barker’s acceptance function is not far from optimal: in the worst case, it increases the asymptotic variance of Monte Carlo averages by at most a factor of two compared with MH. Although MCMC algorithms based on Barker’s acceptance function are no longer strictly speaking Metropolis–Hastings methods, we retain the term Metropolis-adjusted 

(instead of Barker-adjusted ) in this paper for simplicity. Such MCMC techniques can be used to correct for the bias introduced by the ULA algorithm 2. Consider g = 1 for simplicity, then ULA can be thought of as defining a proposal q(ex | x) = 

N  ex; x + h 

> 2

∇ log p(x), h I. We can thus use this proposal within Metropolis–Hastings to sample (asymptotically in the number of iterations) exactly from p. This is a standard approach known as Metropolis-Adjusted Langevin Algorithm (MALA) (Besag, 1994; Roberts and Tweedie, 1996). In the context of diffusion models, while the proposal-ratio term Ht is computable from score evaluations, both the MH and Barker’s acceptance probability rely on a density ratio p(ex)/p (x)

which is not accessible as p is the intractable diffused data distribution at diffusion time t; i.e., 

p = pt in the notation of Section 2.2. So a direct implementation of MALA is not available from score evaluations alone. In Section 3, we show how we can bypass this key issue. 53 Metropolis-Adjusted Diffusion Models 

In this section, we introduce the Metropolis-Adjusted samplers, intended as correctors for diffusion models, that avoid the problem of non-vanishing bias. The challenge with designing these arises from the fact that the target distribution pt at time t is unknown and only available through its score s(t, xt) = ∇x log pt(x). In Section 3.1, we re-express this density ratio using a line integral which we then use in Sections 3.2 and 3.3 to show how one can obtain an unbiased estimate of rpt . We then leverage this to obtain an MCMC algorithm admitting exactly pt as invariant distribution via a two-coin Bernoulli factory (Gonçalves et al., 2017). Section 3.4 provides an alternative lightweight estimate of the integral by a single application of Simpson’s rule on the integrand. A comparison of both procedures is provided in Figure 2. 

## 3.1 A key density ratio identity 

Consider a ULA proposal to sample from pt; i.e., q(ex | x) = N  ex; x + h 

> 2

s(x, t ), h I. Then, as shown in Section 2.4, see equation 8 and equation 9, Metropolis-Adjusted schemes require evaluations of the ratio of proposals, 

log Ht(x, ex) = log 

q(x | ex)

q(ex | x)



= 12h ex − x − h 

> 2

s(x, t ) 2 − 12h x − ex − h 

> 2

s(ex, t ) 2,

as well as the target density ratio rpt (x, ex) = pt(ex)/p t(x). While we can easily compute Ht(x, ex),

rpt can not be obtained as directly. Here, we present two methods to estimate rpt and ultimately perform the Metropolis-Adjusted Diffusion corrector in Algorithm 1, when only the score s(·, t ) is available. Both methods are based on the identity (see Appendix A for a full derivation): 

log( rpt (x, ex)) = 

Z 10

s x + u(ex − x), t , ex − x du, (10) which reduces the problem of estimating rpt (x, ex) to estimating a one-dimensional integral. 

## 3.2 An unbiased estimator of the density ratio 

We now construct a bounded unbiased estimator of the intractable density ratio rpt (x, ex). This is the key object needed by the Bernoulli factory technique presented in Section 3.3 which will then be used to perform exact adjustment. From equation 10, a natural unbiased estimator of this line integral is obtained by computing 

I = ⟨s(x + U (ex − x), t ), ex − x⟩ , U ∼ U ([0 , 1]) , (11) since we have E[I] = log rpt (x, ex). However, directly exponentiating I does not give an unbiased estimator of rpt (x, ex), and furthermore it does not produce a quantity bounded in [0 , 1] as required of the Bernoulli factory technique. To ensure boundedness, we make the following assumption. 

Assumption 3.1. There exists a function C : Rd × Rd → R+ such that, for every x, ex ∈ Rd,

max  

> u∈[0 ,1]

|⟨ s(x + u(ex − x), t ), ex − x⟩| ≤ C(x, ex). (12) 6Algorithm 2 Exact MCMC via two coin algo-rithm 

Input: state x, proposal ex, score function 

s(·, t ), step size h, score bound C(·, ·).Set α′ = (1 + Ht(x, ex) exp( C(x, ex))) −1

while True do if U ≤ α′ for U ∼ U ([0 , 1]) then return REJECT. 

end if 

Sample N ∼ Poisson(2 C(x, ex)) 

for i = 1 to N , W ← 1 do 

Sample R ∼ U ([0 , 1]) .

I ← ⟨ s(x + R(ex − x), t ), ex − x⟩.

W ← W ·

12 + I

> 2C(x,ex)



.

end for if U ≤ W for U ∼ U ([0 , 1]) then return ACCEPT. 

end if end while Algorithm 3 MH estimate by Simpson’s 1/3 rule 

Input: state x, proposal ex, score function 

s(·, t ), step size h, midpoint xmid = 12 (x + ex).Compute es = 16 (s(x, t ) + 4 s(xmid , t ) + s(ex, t )) .Compute bI = ⟨es, ex − x⟩.Compute bα = min {1, exp( bI)Ht(x, ex)}.Sample U ∼ U ([0 , 1]) .

if U ≤ bα then return ACCEPT. 

end if return REJECT. Estimator Unbiased Low Variance Alg. 2 ✓ ×

Alg. 3 × ✓

Figure 2: Properties of integral estimators. Under this assumption, we obtain |I| ≤ C(x, ex) almost surely. To estimate the exponential of the integral, we use a Poisson-truncated power-series estimator. Let N ∼ Poisson (2 C(x, ex)) , let 

I1, . . . , I N be independent copies of I and define the estimator, 

W =

> N

Y

> j=1

12 + Ij

2C(x, ex)



. (13) The intuition is that the random product expands the exponential series for exp {log rpt (x, ex) −

C(x, ex)}, while the rescaling by C(x, ex) keeps each factor within [0 , 1] . In the following lemma, we show that indeed this is an unbiased estimator of rpt (see proof in Appendix B.1). 

Lemma 3.2. Suppose Assumption 3.1, then W ∈ [0 , 1] almost surely and eC(x,ex)E[W ] = rpt (x, ex).

Hence, conditional on W , if we sample V ∼ U ([0 , 1]) and declare an accept when V ≤ W , then the unconditional accept probability is E[W ]. Thus the construction gives access to a coin with unknown win probability e−C(x,ex)rpt (x, ex), without evaluating the density ratio itself. The next subsection uses this coin to obtain an exact Barker accept–reject algorithm. 

## 3.3 A two-coin MCMC algorithm 

We now turn the bounded estimator from Section 3.2 into an exact Barker correction. A Bernoulli factory is a randomized procedure which produces a Bernoulli random variable with a desired probability f (w), while only having access to flips of a coin with unknown probability w (Keane and O’Brien, 1994; Nacu and Peres, 2005). Here, the w-coin is generated by the random variable 

W in Lemma 3.2, and the desired probability is the Barker acceptance probability αB(x, ex) (see equation 8). To simplify notation, we write H = Ht(x, ex), C = C(x, ex) and r = rpt (x, ex). The 7two-coin construction proceeds in repeated rounds. In each round, we first reject immediately with probability, 

α′ = (1 + He C )−1. (14) If this rejection does not occur, we flip a coin with probability W . If the coin succeeds, we accept the proposal; otherwise, we repeat the round. Thus each round has three possible outcomes: 

P(reject in round ) = α′, P(accept in round ) = (1 − α′)e−C r, P(restart ) = (1 − α′)(1 − e−C r).

Summing over the resulting geometric sequence of restarts gives the eventual acceptance probability αB(x, ex). Hence the algorithm simulates the exact Barker accept–reject decision without evaluating rpt (x, ex). We state this result rigorously in the following theorem (see proof in Appendix B.2). 

Theorem 3.3. Suppose that s(·, t ) = ∇ log pt(·) for some positive differentiable density pt and that Assumption 3.1 holds. Then Algorithm 2 almost surely terminates in finitely many iterations and returns an accept with probability αB(x, ex) (see equation 8). Consequently, the adjusted proposal in Algorithm 1 using Algorithm 2 is reversible with respect to pt and hence, the algorithm produces samples that converge in total variation distance to pt.

To determine the number of iterations required for Algorithm 2 to terminate, we state the following proposition which characterises its iteration complexity (see proof in Appendix B.3). 

Proposition 3.4. In the setting of Theorem 3.3, the number of iterations before Algorithm 2 termi-nates is distributed as a geometric distribution with probability (1 + Hr )/(1 + He C ). Furthermore, the expected number of score queries used by the algorithm is (2 CHe C )/(1 + Hr ).

The computational cost of Algorithm 2 is governed by the tightness of C as a bound on | log (r)|.If it is quite a tight bound, one can expect very few iterations. A loose bound C ≫ | log r| is costly, since the expected number of score queries grows as eC . For small Langevin step sizes h, the regime C ≪ 1 arises naturally. Since C must bound the inner product ⟨s(x + U (ex − x), t ), ex − x⟩,it scales with ∥ex − x∥ = O(( hd )1/2). In this regime, the expected number of score queries reduces to approximately 2CHe C /(1 + Hr ) = O(( hd )1/2), decaying to zero as the step size is reduced. We conclude with a brief discussion of implementation considerations for Algorithm 2. 

Choosing C. Properly selecting C(x, ex) so that Assumption 3.1 holds is essential for Algo-rithm 2. Two natural settings yield computable choices: (i) when the denoising function associated with the score is bounded—for example, when the data is compactly supported, or in image domains, where clipping the denoiser to the data range is standard practice; (ii) when the score is Lipschitz in x. In Appendix D, we provide directly computable choices for C in these settings. 

Choosing h. The step size controls a familiar tradeoff between mixing speed and rejection rate. We show that in the high-dimensional limit with a Gaussian target, the optimal step size scales as h ∼ d−1/3 and yields an asymptotic acceptance rate of 0.347 (see Appendix F). This rate provides a practical heuristic: tune h to achieve an observed acceptance rate of roughly one-third. 

## 3.4 Newton–Cotes quadrature approximation 

While Algorithm 2 gives an exact Barker correction, it requires a random number of score evaluations. A cheaper alternative is to approximate the line integral in equation 10 deterministically with a Newton–Cotes quadrature rule, and then plug the result into the usual Metropolis–Hastings 8acceptance probability. A Newton–Cotes quadrature rule with N + 1 equally spaced points approximates this integral using 

ˆI(N +1) ∆ =

> N

X

> i=0

wi s  x + iN (ex − x), t  , ex − x , (15) where i = 0 , 1, . . . , N and PNi=0 wi = 1 . This gives rise to well-known quadrature rules such as the trapezoidal rule and Simpson’s 1/3 rule, where truncation errors are described by derivatives of the function f (u) = s x + u(ex − x), t , ex − x (i.e. the integrand of Equation equation 10). For a survey of Newton-Cotes quadrature rules and their truncation errors, we refer the reader to Atkinson (1989). 

Example 3.1 (Simpson’s 1/3 Rule) . Setting N = 2 , w0 = w2 = 16 and w1 = 46 yields the estimate 

ˆI(3) ∆ = 16⟨s(x, t ) + 4 s(xmid , t ) + s(ex, t ), ex − x⟩, xmid = 12(x + ex).

This has truncation error − 12880 f (4) (u∗) for some u∗ ∈ [0 , 1] , and is of order O(h5/2).With an estimate from Equation 15 in hand, we can use it in place of rpt in the Metropolis-Hastings acceptance probability ˆαMH (x, ex) = min 1, exp ( ˆI∆)Ht(x, ex) . Empirically, we found Simpson’s 1/3 rule effective. This yields Algorithm 3. Other Newton–Cotes quadrature rules could be used to approximate ˆI. The estimate only requires access to the score and enjoys two advantages over the exact method. The first is that it is more efficient to compute as other quantities including the weights {wi}Ni=0 are known. The second is that it provides a closed form estimate of the acceptance probability. The main disadvantage is that it is a biased estimate of Equation equation 10 due to truncation. 

# 4 Related work 

Bernoulli Factories. For diffusion models, (Chen et al., 2026) used a Bernoulli factory to sample from pt|t+δ(x|x′) ∝ pt(x) exp (−|| x − αtx′|| 2/(2 σ2 

> t

) when pt is only known through its score. This 

couples each predictor step with a rejection sampling step, with the latter acting as the corrector. This differs from our work as MADM corrector steps are optional and separate from predictor steps. Bernoulli factories have also been used in MCMC to sample intractable posteriors (Herbei and Berliner, 2014; Gonçalves et al., 2017; Morina et al., 2022; Vats et al., 2022; Gonçalves et al., 2023). 

Soft Metropolis–Hastings Adjustments. Under the assumption that pt ≈ pt+1 , Feng et al. (2025) similarly suggested using each predictor step as a proposal, and interpolating between the proposal and current state with the MH acceptance probability. The latter relaxes the hard update from MH adjustments to a soft update. This differs from our work as we make no assumptions on consecutive marginals and apply MH adjustments to corrector steps (i.e., x(i+1)  

> t

only targets pt). 

Metropolis–Hastings Adjustments for Compositional Models. (Sjöberg et al., 2026) proposed estimating Equation 10 with the trapezoidal rule for MH adjustments in compositional models, such as guided diffusion models. While their trapezoidal rule and our Simpson’s rule are similar ways of estimating Equation 10, as both are instances of Newton-Cotes quadrature rules in Section 3.4, we emphasise that their FIDs on ImageNet are not competitive and comparisons against 9Figure 3: DDM sampling using an ancestral sampler predictor, and MADM correction. Mean distance to the true samples and the 95% sample containment distance are shown. other image baselines in Karras et al. (2022) are not provided. The focus of our work differs from theirs as we extend the Predictor-Corrector framework of (Song et al., 2021) with MH adjustments to corrector steps for both unconditional and class-conditional models, and demonstrate their effectiveness through consistent gains in FID over all deterministic sampling baselines in Karras et al. (2022). 

Learning Acceptance Probabilities. (Aloui et al., 2025) proposed learning acceptance probabilities αMH by training an additional neural network αθ with a loss to enforce detailed balance. This differs from MADM as calculations of acceptance probabilities are either bypassed (Algorithm 2) or performed directly with suitable approximations of the density ratio (Algorithm 3); enabling the use of MH adjustments while only requiring access to a pre-trained score model. 

# 5 Experiments 

## 5.1 Protocol 

Our proposed MADM corrector samplers only require access to a score function. This makes them readily applicable to any pre-trained diffusion model. The aim of our experiments was to illustrate the inherent gap in sample quality in existing PC samplers, and to demonstrate the effectiveness of MADM corrections to remedy this. We considered two types of datasets for our experiments: 2D datasets and image datasets. On 2D datasets, we showed that the ancestral sampler generated samples that are off the support of underlying data distributions, and that MADM corrector steps move these outliers back to the support. On image datasets, we demonstrated numerical improvements in FID by interweaving probability flow ODE steps and MADM corrector steps. 10 5.2 Synthetic 2D Datasets 

We considered the ancestral sampler as the predictor on four 2D datasets: spiral, funnel, Sierpinski triangle, and pinwheel. It is known that the ancestral sampler is a specific discretisation of Equation 4 (see Appendix E of Song et al. (2021)). In other words, we can view one step of the ancestral sampler as equivalent to: one probability flow ODE predictor step and one unadjusted Langevin corrector step (i.e. ULA) with a shared step size. Our results with these settings are shown in Figure 3. The second row in Figure 3 illustrates that the ULA step embedded within each ancestral sampler step is not sufficient to move generated samples to the support of underlying data distributions, leaving a large number of outliers. The third row in Figure 3 shows that MADM corrector steps, using a hybrid sampling strategy (see Appendix E), allow us to move samples to the support of underlying data distributions, leaving few to no outliers. This illustrates an improvement in sample quality by adding MH adjustments within the sampling process. 

## 5.3 Image Datasets 

We considered the probability flow ODE as the predictor on four standard image datasets: CIFAR-10 (Krizhevsky and Hinton, 2009), FFHQ (Karras et al., 2019), AFHQv2 (Choi et al., 2020) and ImageNet-64 (Deng et al., 2009). We used the pre-trained models and default sampling strategy from Karras et al. (2022) and evaluated sample quality using FID. The Heun ODE solver served as the natural and scalable baseline for image datasets, as deterministic sampling is often preferred for efficiency. We also compared against the Euler ODE solver, as it often serves as a starting point in model development for ODE-based models. Finally, we used 1 MADM corrector step per predictor step, to have a closer analogue of PC sampler experiments in Song et al. (2021) with MADM corrections. Due to budget constraints of academic computing and the exact MCMC algorithm (Algorithm 2) requiring many score evaluations, we focus on MH estimates by Simpson’s rule (Algorithm 3) in our experiments. Our FID results with these settings are shown in Table 1. For the unconditional models on CIFAR-10, FFHQ and AFHQv2, applying one MADM corrector step yielded FIDs competitive to those in Karras et al. (2022); Hatamizadeh et al. (2025). While some gains were modest, we note that they were consistent across all three datasets and ODE solvers. This suggests that MADM improves or matches the sample quality from deterministic sampling. For the class-conditional ImageNet-64 model, we saw that applying one MADM corrector step per Euler predictor step yielded larger gains in FID than using only Heun predictor steps without corrector steps. This suggests that it is more beneficial to use MH adjustments, rather than a higher-order ODE solver, to mitigate discretisation errors when the number of discretisation steps is large. 

# 6 Conclusion 

We have introduced MADM as a Metropolis-adjusted corrector framework that naturally extends the Predictor-Corrector framework of diffusion models, to mitigate discretisation bias. We presented the first exact method to compute Barker-adjusted Langevin corrections for diffusion models, based on a two-coin Bernoulli factory algorithm. We also presented an efficient approximation to compute Metropolis-adjusted Langevin corrections, based on Simpson’s 1/3 rule. Empirically, we demonstrated their effectiveness for improving sample quality on both synthetic and image datasets, with consistent gains in FID on the latter. Regarding limitations, we note that the exact 11 Table 1: Fréchet Inception Distance (FID) on image datasets sampled using the probability flow ODE (PF-ODE) and MADM with pre-trained models from Karras et al. (2022). All FIDs are calculated using 50,000 samples. We highlight the best FID in bold. ODE Solver PC Sampler CIFAR-10 FFHQ AFHQv2 ImageNet Heun PF-ODE 1.96 2.47 2.04 2.32 PF-ODE + MADM 1.95 2.43 2.03 2.15 

Euler PF-ODE 7.62 4.59 2.93 2.41 PF-ODE + MADM 7.52 4.52 2.91 2.25 

method can be computationally expensive due to the random number of score evaluations required; however, we found that using Simpson’s rule can partially mitigate this. Furthermore, our work assumes perfect score estimation, though we note this is not unique to our work and is an inherent limitation of diffusion models. Future work can focus on developing efficient variants of our exact method or its subroutines. 

# 7 Acknowledgements 

We acknowledge the use of resources provided by the Isambard-AI National AI Research Resource (AIRR) (McIntosh-Smith et al., 2024). Isambard-AI is operated by the University of Bristol and is funded by the UK Government’s Department for Science, Innovation and Technology (DSIT) via UK Research and Innovation; and the Science and Technology Facilities Council [ST/AIRR/I-A-I/1023]. Kevin H. Lam gratefully acknowledges his PhD funding from Google DeepMind. Tyler Farghly is supported by the European Research Council Starting Grant DYNASTY - 101039676. Christopher Williams acknowledges support from the Defence Science and Technology (DST) Group Australia. Jun Yang acknowledges support from the Independent Research Fund Denmark (DFF) through the Sapere Aude Starting Grant (5251-00032B). 

# References 

Sanket Agrawal, Dootika Vats, Krzysztof Łatuszyński, and Gareth O Roberts. Optimal scaling of MCMC beyond Metropolis. Advances in Applied Probability , 55(2):492–509, 2023. Ahmed Aloui, Ali Hasan, Juncheng Dong, Zihao Wu, and Vahid Tarokh. Score-based Metropolis-Hastings algorithms. arXiv preprint arXiv:2501.00467 , 2025. Kendall E. Atkinson. An Introduction to Numerical Analysis . Wiley, New York ;, 2nd ed. edition, 1989. Julian Besag. Comments on “Representations of knowledge in complex systems” by U. Grenander and M. I. Miller. Journal of the Royal Statistical Society Series B , 56(591-592):4, 1994. Fan Chen, Sinho Chewi, Constantinos Daskalakis, and Alexander Rakhlin. High-accuracy sampling for diffusion models and log-concave distributions. arXiv preprint arXiv:2602.01338 , 2026. 12 Yunjey Choi, Youngjung Uh, Jaejun Yoo, and Jung-Woo Ha. Stargan v2: Diverse image synthesis for multiple domains. In IEEE/CVF Conference on Computer Vision and Pattern Recognition (CVPR) , June 2020. Jia Deng, Wei Dong, Richard Socher, Li-Jia Li, Kai Li, and Li Fei-Fei. Imagenet: A large-scale hierarchical image database. In 2009 IEEE Conference on Computer Vision and Pattern Recognition , pages 248–255, 2009. Alain Durmus and Eric Moulines. Nonasymptotic convergence analysis for the unadjusted Langevin algorithm. Annals of Applied Probability , 27(3):1551–1587, 2017. Hanqi Feng, Peng Qiu, Meng-Chun Zhang, You Fan, Yiran Tao, and Barnabas Poczos. Soft Metropolis-Hastings correction for generative model sampling. bioRxiv , 2025. Flávio B Gonçalves, Krzysztof Łatuszyński, and Gareth O Roberts. Exact Monte Carlo likelihood-based inference for jump-diffusion processes. Journal of the Royal Statistical Society Series B: Statistical Methodology , 85(3):732–756, 2023. Flávio B Gonçalves, Krzysztof Łatuszyński, and Gareth O Roberts. Barker’s algorithm for Bayesian inference with intractable likelihoods. Brazilian Journal of Probability and Statistics , 31(4): 732–745, 2017. Ali Hatamizadeh, Jiaming Song, Guilin Liu, Jan Kautz, and Arash Vahdat. Diffit: Diffusion vision transformers for image generation. In Computer Vision – ECCV 2024 , pages 37–55, Cham, 2025. Springer Nature Switzerland. ISBN 978-3-031-73242-3. Radu Herbei and L Mark Berliner. Estimating ocean circulation: an MCMC approach with ap-proximated likelihoods via the Bernoulli factory. Journal of the American Statistical Association ,109(507):944–954, 2014. Jonathan Ho, Ajay Jain, and Pieter Abbeel. Denoising diffusion probabilistic models. In Advances in Neural Information Processing Systems , 2020. Tero Karras, Samuli Laine, and Timo Aila. A style-based generator architecture for generative adversarial networks. In Proceedings of the IEEE/CVF Conference on Computer Vision and Pattern Recognition (CVPR) , June 2019. Tero Karras, Miika Aittala, Timo Aila, and Samuli Laine. Elucidating the design space of diffusion-based generative models. In Advances in Neural Information Processing Systems , 2022. MS Keane and George L O’Brien. A Bernoulli factory. ACM Transactions on Modeling and Computer Simulation (TOMACS) , 4(2):213–219, 1994. Alex Krizhevsky and Geoffrey Hinton. Learning multiple layers of features from tiny images. Technical report, University of Toronto, Toronto, Ontario, 2009. Krzysztof Łatuszyński and Gareth O Roberts. CLTs and asymptotic variance of time-sampled Markov chains. Methodology and Computing in Applied Probability , 15(1):237–247, 2013. Simon McIntosh-Smith, Sadaf R. Alam, and Christopher J. Woods. Isambard-ai: a leadership-class supercomputer optimised specifically for artificial intelligence. In Proceedings of the Cray User Group, CUG 2024, Perth, WA, Australia, May 5-9, 2024 , pages 44–54. ACM, 2024. 13 Giulio Morina, Krzysztof Łatuszyński, Piotr Nayar, and Alex Wendland. From the Bernoulli factory to a dice enterprise via perfect sampling of Markov chains. The Annals of Applied Probability ,32(1):327–359, 2022. Şerban Nacu and Yuval Peres. Fast simulation of new coins from old. Annals of Applied Probability ,pages 93–115, 2005. Peter H Peskun. Optimum Monte-Carlo sampling using Markov chains. Biometrika , 60(3):607–612, 1973. Gareth O. Roberts and Jeffrey S. Rosenthal. Optimal scaling of discrete approximations to Langevin diffusions. Journal of the Royal Statistical Society. Series B (Statistical Methodology) , 60(1): 255–268, 1998. Gareth O. Roberts and Jeffrey S. Rosenthal. Optimal scaling for various Metropolis–Hastings algorithms. Statistical Science , 16(4):351 – 367, 2001. Gareth O. Roberts and Jeffrey S. Rosenthal. Harris recurrence of Metropolis-within-Gibbs and trans-dimensional Markov chains. The Annals of Applied Probability , 16(4):2123–2139, 2006. Gareth O Roberts and Richard L Tweedie. Exponential convergence of Langevin distributions and their discrete approximations. Bernoulli , 2(3):341–363, 1996. Gareth O. Roberts, Andrew Gelman, and Wally R. Gilks. Weak convergence and optimal scaling of random walk Metropolis algorithms. The Annals of Applied Probability , 7(1):110–120, 1997. Anders Sjöberg, Jakob Lindqvist, Magnus Önnheim, Mats Jirstrand, and Lennart Svensson. MCMC correction of score-based diffusion models for model composition. Entropy , 28(3), 2026. Jascha Sohl-Dickstein, Eric Weiss, Niru Maheswaranathan, and Surya Ganguli. Deep unsupervised learning using nonequilibrium thermodynamics. In International Conference on Machine Learning ,2015. Yang Song and Stefano Ermon. Generative modeling by estimating gradients of the data distribution. In Advances in Neural Information Processing Systems , 2019. Yang Song, Jascha Sohl-Dickstein, Diederik P Kingma, Abhishek Kumar, Stefano Ermon, and Ben Poole. Score-based generative modeling through stochastic differential equations. In International Conference on Learning Representations , 2021. Dootika Vats, Flávio B Gonçalves, K Łatuszyński, and Gareth O Roberts. Efficient Bernoulli factory Markov chain Monte Carlo for intractable posteriors. Biometrika , 109(2):369–385, 2022. Pascal Vincent. A connection between score matching and denoising autoencoders. Neural Computation , 23(7):1661–1674, 2011. Christopher Williams, Andrew Campbell, Arnaud Doucet, and Saifuddin Syed. Score-optimal diffusion schedules. In Advances in Neural Information Processing Systems , 2024. Jun Yang, Gareth O. Roberts, and Jeffrey S. Rosenthal. Optimal scaling of random-walk Metropolis algorithms on general target distributions. Stochastic Processes and their Applications , 130(10): 6094–6132, 2020. 14 A Derivation of density ratio identity 

Recall that rpt (x, ex) = pt(ex) 

> pt(x)

, so it follows that log rpt (x, ex) = log pt(ex) − log pt(x). Note that p is differentiable. By the fundamental theorem of calculus, we can write 

log pt(ex) − log pt(x) = 

Z 10

∇ log pt

 x + u(ex − x), ex − x du

=

Z 10

s x + u(ex − x), t , ex − x du, 

where the second equality follows from setting s(·, t ) = ∇ log pt. This completes our derivation of Equation equation 10. 

# B Proofs for the exact estimator 

For the sake of brevity, we use the shorthand H = Ht(x, ˜x), C = C(x, ˜x), r = rpt (x, ˜x).

## B.1 Proof of Lemma 3.2 

By Assumption 3.1, we have that |Ii| ≤ C holds almost surely, for all i, and hence, 

12 + Ii

2C ∈ [0 , 1] .

Since W is formed of a product of these variables, we also obtain W ∈ [0 , 1] almost surely. Since E[Ii] = log (r) by equation 10, and {Ii}Ni=1 are i.i.d. and also independent of N , we obtain, 

E[W |N ] = 

> N

Y

> i=1

E

12 + Ii

2C



=

12 + log( r)2C

N

,

where we use the convention 00 = 1 . Recalling that N ∼ Poisson (2 C), the law of total expectation gives, 

E[W ] = 

> ∞

X

> n=0

(2 C)n exp( −2C)

n! E[W |N = n]= exp( −2C)

> ∞

X

> n=0

1

n!

 C + log( r)n.

Finally, recognising the sum as the Taylor series of the exponential, we obtain, 

E[W ] = exp( −2C) exp( C + log( r)) = exp( −C)r, 

completing the proof. 15 B.2 Proof of Theorem 3.3 

In each outer iteration of Algorithm 2, one of three events occurs. The iteration could produce an immediate rejection, which results from a coin flip with probability α′. The iteration could also produce an acceptance, which requires first avoiding rejection and then winning the following acceptance coin flip with probability W . Since the immediate rejection flip and the acceptance flip are independent, the tower property gives this probability as (1 − α′)E[W ] = (1 − α′) exp (−C)r.Finally, it could occur that neither coin wins and neither a rejection or acceptance is returned, resulting in the loop restarting. This occurs with the remaining probability, 

1 − α′ − (1 − α′) exp( −C)r = (1 − α′)(1 − exp( −C)r).

We begin by briefly analysing the number of iterations (of the outer loop) before termination, which we denote by K. Since K being at least as large as n is equivalent to the event that the first 

n − 1 iterations produced restarts, it follows that, 

P(K ≥ n) = (1 − α′)n−1(1 − exp( −C)r)n−1.

Since H > 0, we have α′ < 1 and so the restart probability satisfies (1 − α′)(1 − exp (−C)r) ∈ [0 , 1) .Hence we have lim n→∞ P(K ≥ n) = 0 . To show that K is almost-surely finite, we note that for any n,

P(K < ∞) ≥ P(K < n ) = 1 − P(K ≥ n).

In particular, we have that 

P(K < ∞) ≥ 1 − lim  

> n→∞

P(K ≥ n) = 1 .

We now compute the probability of acceptance. Let An denote the events that the algorithm accepts on the n-th outer iteration. Since An necessarily requires a sequence of n − 1 no decision iterations followed by an acceptance, it follows directly that, 

Pr( An) = (1 − α′)n−1(1 − exp( −C)r)n−1(1 − α′) exp( −C)r. 

To compute the probability that the algorithm terminates with an acceptance, we must compute the probability of the union ∪∞

> n=1

An. Since the events {An}∞ 

> n=1

are disjoint, we obtain the acceptance probability as a geometric series: 

Pr 

 ∞[

> n=1

An



=

> ∞

X

> n=1

Pr( An)= (1 − α′) exp( −C)r

> ∞

X

> n=1

(1 − α′)n−1(1 − exp( −C)r)n−1.

Since the sum is a geometric series with rate (1 − α′)(1 − exp (−C)r) ∈ [0 , 1) , it follows that the sum converges with, 

Pr 

 ∞[

> n=1

An



= (1 − α′) exp( −C)r 11 − (1 − α′)(1 − exp( −C)r)= (1 − α′) exp( −C)rα′ + (1 − α′) exp( −C)r .

16 Recalling that α′ = (1 + H exp( C)) −1, this gives, 

Pr 

 ∞[

> n=1

An



= H r 

1 + H r = αB (x, ˜x).

Reversibility with respect to pt follows since the Barker acceptance function satisfies aB (r) = 

ra B (1 /r ) for all r > 0, which is the sufficient condition for detailed balance, as established in Section 2.4. For convergence, we use that pt is a positive density and that the Langevin proposal has full support on Rd, which guarantees αB (x, ˜x) > 0 everywhere and hence that the chain is ϕ-irreducible with respect to Lebesgue measure. Then Algorithm 1 is Harris recurrent, which follows from the same line of argument as in Roberts and Rosenthal (2006). Since pt is an invariant probability measure, the chain is positive recurrent. Convergence in total variation distance to pt follows. 

## B.3 Proof of Proposition 3.4 

From the proof of Theorem 3.3, the probability of termination in a single outer iteration is, 

α′ + (1 − α′)e−C r = 11 + He C + Hr 

1 + He C = 1 + Hr 

1 + He C ,

and since iterations are independent, the number of iterations K before termination is geometric with this probability, establishing the first claim. For the second claim, we reframe the setting as an optional stopping problem. Let Ni denote the number of score queries at iteration i where we set Ni = 0 if a rejection is returned at that iteration. With this, we obtain that the total number of score queries is given by E

 PKi=1 Ni



.For i > K , we continue to generate Ni as if the algorithm has not terminated so that (Ni)∞

> i=1

is an i.i.d. sequence. Let Fn denote the sigma-algebra generated by all randomness in iterations 

1, . . . , n . From this, we see that (Ni)∞ 

> i=1

is adapted to the filtration (Fi)∞ 

> i=1

and K is a stopping time. With this, we describe the total number of queries as an optionally stopped sum over an i.i.d. process, and hence we can apply Wald’s lemma. Provided E[N1], E[K] < ∞, this lemma gives, 

E

 KX

> i=1

Ni



= E[N1]E[K].

Since N1 takes the value 0 with probability α′ and is Poisson (2 C) distributed with probability 

1 − α′, we obtain the expectation 

E[N1] = 2(1 − α′)C = 2CHe C

1 + He C .

Since K is geometrically distributed with parameter (1 + Hr )/(1 + He C ), we also obtain, 

E[K] = 1 + He C

1 + Hr .

Thus, we obtain the expected number of queries from the product, 

E

 KX

> i=1

Ni



= 2CHe C

1 + He C · 1 + He C

1 + Hr = 2CHe C

1 + Hr .

17 C Results for Newton–Cotes quadrature rules 

## C.1 Other common quadrature rules 

Example C.1 (Trapezoidal Rule) . Setting N = 1 and w0 = w1 = 12 yields the estimate 

ˆI(2) ∆ = 12⟨s(x, t ) + s(˜ x, t ), ˜x − x⟩.

This has truncation error − 112 f ′′ (u∗) for some u∗ ∈ [0 , 1] , and is of order O(h3/2).

Example C.2 (Simpson’s 3/8 rule) . Setting N = 3 , w0 = w3 = 18 and w1 = w2 = 38 yields the estimate 

ˆI(4) ∆ = 18⟨s(x, t ) + 3 s(y, t ) + 3 s(z, t ) + s(˜ x, t ), ˜x − x⟩, y = x + 13(˜ x − x), z = x + 23(˜ x − x).

This has truncation error − 16480 f (4) (u∗) for some u∗ ∈ [0 , 1] , and is of order O(h5/2).

## C.2 Derivation of error bounds from truncation error 

Let v = ˜ x − x. The results in this section are based on the following lemma. 

Lemma C.1. Let ˜x = x + h 

> 2

s(x, t ) + √hz where z ∼ N (0, I). Suppose s(x, t ) is locally bounded. Then ∥˜x − x∥ has O(h1/2).Proof. By triangle inequality, we have 

∥v∥ = ∥˜x − x∥ ≤ h

2 ∥s(x, t )∥ + √h∥z∥.

Since s(x, t ) is locally bounded, the first term on the RHS of the inequality has order O(h). The second term on the RHS of the inequality has order O(h1/2). Since h1/2 dominates h as h → 0, it follows that ∥v∥ has order O(h1/2).Recall from Section 3.4 that 

f : [0 , 1] → R, f (u) = s x + uv, t , v

where f is the integrand of Equation equation 10. Truncation errors from the Newton–Cotes quadrature rules introduced in Section 3.4 are expressed by derivatives of f . More specifically, they can be expressed by Fréchet derivatives of s evaluated at x + u∗v. The n-th Fréchet derivative 

Dns(x) : Rd × · · · × Rd

| {z } 

> ntimes

→ Rd is a multi-linear map takes n arguments from Rd and outputs a value from Rd.

Trapezoidal rule. Recall from Example C.1 that the truncation error of the trapezoidal rule is 

− 112 f ′′ (u∗) for some u∗ ∈ [0 , 1] . This gives us 

|f ′′ (u∗)| = |⟨ D2s(x + u∗v)[ v, v], v⟩| ≤ ∥ D2s(x + u∗v)[ v, v]∥∥ v∥≤ ∥ D2s(x + u∗v)∥op ∥v∥3

where the first inequality follows from Cauchy-Schwarz and the second follows from the definition of operator norms for multi-linear maps. By Lemma C.1, it follows that |f ′′ (u∗)| has O(h3/2).18 Simpson’s 1/3 and 3/8 rules. Recall from Examples 3.1 and C.2 that the truncation error of both Simpson’s 1/3 and 3/8 rules are proportional f (4) (u∗) for some u∗ ∈ [0 , 1] . Following the same arguments as for the trapezoidal rule, this gives us 

|f (4) (u∗)| = |⟨ D4s(x + u∗v)[ v, v, v, v], v⟩| ≤ ∥ D4s(x + u∗v)[ v, v, v, v]∥∥ v∥≤ ∥ D4s(x + u∗v)∥op ∥v∥5.

By Lemma C.1, it follows that |f (4) (u∗)| has O(h5/2).

# D Choosing the quantity C

The validity of Algorithm 2 rests on a valid estimate of C(x, ex) and, as shown in Proposition 3.4, the computational cost depends on the tightness of this quantity. Choosing a suitable C(x, ˜x)

requires some additional knowledge about the score function as it bounds the score function along some, typically small, line segment. Thus, we require some predictability of how the score function changes along this line segment via smoothness or boundedness of the score network. 

Bounded data support. Suppose that the data distribution is known to be supported on the centred ball of radius b. By Tweedie’s identity, the score admits the form, 

∇ log pt = rt d(x, t ) − x

r2 

> t

σ2

> t

, d(x, t ) := E[X0|Xt = x].

Thus, the score function can be controlled using the fact that ∥d(x, t )∥ ≤ b. Similarly, given a generic score function s(x, t ) that is, for example, derived from a trained neural network, it may be parameterised such that the associated denoising function ˆd(x, t ) is appropriately compact. Indeed, if we assume that ∥ ˆd(·, t )∥ ≤ b for some b > 0, it follows that for any x, ex ∈ Rd and u ∈ [0 , 1] ,

|⟨ s(x + u(ex − x), t ), ex − x⟩| = 1

r2 

> t

σ2

> t

|rt⟨d(x + u(ex − x), t ), ex − x⟩ − ⟨ x + u(ex − x), ex − x⟩| ≤ 1

rtσ2

> t

∥d(x + u(ex − x), t )∥∥ ex − x∥ + 1

r2 

> t

σ2

> t

|⟨ x + u(ex − x), ex − x⟩| ≤ br t + max {∥ x∥, ∥ex∥} 

r2 

> t

σ2

> t

∥ex − x∥.

Thus, setting C(x, ex) to be this value on the right-hand side verifies Assumption 3.1. 

Lipschitz score. The previous approach relied on boundedness in the score. Here we utilise first-order smoothness, by supposing that the score function s(x, t ) is Lipschitz in x. This means that there exists some constant L ≥ 0 such that, 

∥s(x, t ) − s(ex, t )∥ ≤ L∥x − ex∥,

for all x, ex. Thus, whenever u ∈ [0 , 0.5] , we have that, 

|⟨ s(x + u(ex − x), t ), ex − x⟩| ≤ ∥ s(x + u(ex − x), t )∥∥ ex − x∥≤ ∥ s(x, t )∥∥ ex − x∥ + ∥s(x + u(ex − x), t ) − s(x, t )∥∥ ex − x∥≤ ∥ s(x, t )∥∥ ex − x∥ + L

2 ∥ex − x∥2.

19 Similarly, for u ∈ [0 .5, 1] , we obtain, 

|⟨ s(x + u(ex − x), t ), ex − x⟩| ≤ ∥ s(ex, t )∥∥ ex − x∥ + L

2 ∥ex − x∥2.

Thus, Assumption 3.1 holds with constant, 

C(x, ex) = max {∥ s(x, t )∥, ∥s(ex, t )∥}∥ ex − x∥ + L

2 ∥ex − x∥2.

Note that in fact, this proof only requires the weaker one-sided Lipschitz condition given by, 

|⟨ s(x, t ) − s(ex, t ), x − ex⟩| ≤ L∥x − ex∥2.

# E Practical termination 

Algorithm 4: Hybrid MADM algorithm                                      

> Input: x,ex,s(·, t ),h,C(·,·).Set α′= (1 + Ht(x,ex) exp( C(x,ex))) −1
> for k= 0 , . . . , K −1do if U≤α′for U∼ U ([0 ,1]) then return REJECT.
> end if
> Compute Was in Alg. 2
> if U≤Wfor U∼ U ([0 ,1]) then return ACCEPT.
> end if end for return Alg. 3

Algorithm 2 gives an exact method and avoids dis-cretisation bias, but may require many score evalua-tions. Algorithm 3 is biased since it perturbs the ac-ceptance probability, but it is computationally much cheaper. To get the best of both worlds, we propose a hybrid sampling algorithm: attempt Algorithm 2 K times to get an accept or reject decision; use Algorithm 3 if this fails. This controls the worst-case computational cost while retaining an exact update whenever the Bernoulli factory terminates in time. In our experiments, we used this hybrid sampling strategy for synthetic 2D datasets, where it is feasi-ble, and showed that on image datasets, using only Algorithm 3 still yields performance gains. 

# F Optimal scaling 

Optimal scaling theory addresses the choice of step size h to achieve efficient exploration of the target: if proposed moves are too small, the chain mixes slowly, whereas overly large proposals lead to frequent rejections and poor exploration. The goal is to strike an optimal balance by selecting a proposal step size h that maximizes sampling efficiency. We establish the optimal scaling of h in Algorithm 2 by maximizing the Expected Squared Jumping Distance (ESJD) in the stationary phase: 

max  

> h

ESJD (h) := max  

> h

Ex(i)  

> t∼pt

[∥x(i+1)  

> t

− x(i) 

> t

∥2].

The justification for using ESJD as an optimization criterion comes from its connection to diffusion limits (Roberts et al., 1997). When weak convergence to a diffusion limit is established, the ESJD of the Markov chain converges to the quadratic variation of the limiting diffusion (Roberts and Rosenthal, 2001; Yang et al., 2020). A larger ESJD generally indicates that the chain is exploring the state space more efficiently, avoiding both overly conservative moves and excessive rejection. We establish the optimal scaling theory for the Barker-adjusted Langevin algorithm in Algo-rithm 2. For clarity, we present the result for multivariate Gaussian targets. The argument extends in a standard way to broader classes of target distributions (Yang et al., 2020), with the same limiting optimal acceptance rate. 20 Proposition F.1. Assume that pt is a d-dimensional standard multivariate Gaussian and that the score is exact, i.e., s(·, t ) = ∇ log pt(·). Choosing h to maximize the ESJD for Algorithm 2, then the optimal scaling for h is h = hd ∼ d−1/3, and the optimal expected acceptance rate converges to 

0.347 (to three decimal places) as d → ∞ .

Note that if h scales with d faster (or slower) than order d−1/3, the limiting acceptance probability becomes 1/2 (or 0), which cannot be optimal for maximizing the ESJD. Therefore, we will write 

h = hd = ℓ2d−1/3 for some ℓ > 0. The proof follows the same line of argument as in Roberts and Rosenthal (1998), with the acceptance function modified according to Agrawal et al. (2023). First, for notational clarity, we suppress the time index in the target pt. By assumption, 

pt(x) = 

> d

Y

> i=1

ϕ(xi), where ϕ(xi) = (2 π)−1/2 exp( −x2 

> i

/2) .

Under a perfect score, the resulting exact algorithm reduces to the Barker-adjusted Langevin algorithm: proposals are generated via ULA and accepted according to Barker’s rule. For consistency in notation, we also suppress the subscript B in the acceptance probability αB and write αd to highlight its dependence on the dimension. That is, 

αd(x, ˜x) = 11 + exp( −Wd) (16) where Wd = log 

pt(˜ x)q(x|˜x)

> pt(x)q(˜ x|x)



is the log-acceptance ratio, and the ULA proposal 

˜xi = xi − h

2 xi + √hz i, i = 1 , . . . , d, (17) where xi, z iiid 

∼ N (0 , 1) in stationarity. Note that while the assumption of a multivariate Gaussian target simplifies the analysis of ULA proposal, the argument extends readily to more general settings (Yang et al., 2020). We restrict attention to the Gaussian case only for simplicity. We are interested in maximizing the ESJD, with step size scales with dimension as h = ℓ2d−1/3

for a constant ℓ > 0 to yield a non-degenerate limit: ESJD d(h) = E[∥x − ˜x∥2αd(x, ˜x)] = d · E (˜ x1 − x1)2αd(x, ˜x) (18) As we re-parameterize h by ℓ via h = ℓ2d−1/3, we can define an efficiency function over ℓ:

Jd(ℓ) := d−2/3 · ESJD d(h). (19) Then maximizing ESJD (h) over h is equivalent to maximizing Jd(ℓ) over ℓ up to a constant d−2/3.Next, we establish the limiting distribution of Wd. As shown in standard algebraic expansions for Gaussian targets, the log-ratio simplifies perfectly to: 

Wd =

> d

X

> i=1

ξi,d , where ξi,d = h

8 (x2 

> i

− ˜x2 

> i

) (20) 

Lemma F.2. As d → ∞ , Wd

> D

−→ N 



− ℓ6 

> 32

, ℓ6

> 16



.

21 Proof. The proof follows the same line of arguments as the proof for MALA in Roberts and Rosenthal (1998). Substituting the ULA proposal into ξi,d , we obtain: 

ξi,d = h

8



hx 2 

> i

− h2

4 x2 

> i

− 2√h



1 − h

2



xizi − hz 2

> i



. (21) Taking expectations using E[x2 

> i

] = 1 , E[z2 

> i

] = 1 , and E[xizi] = 0 :

μd = E[ξi,d ] = h

8



h − h2

4 − h



= − h3

32 . (22) For the variance, the dominant term is the cross-term −h3/2 

> 4

xizi. Thus: 

Var( ξi,d ) = E

"

−h3/2

4 xizi

2#

+ O(h4) = h3

16 + O(h4). (23) Because h = ℓ2d−1/3, the sum of means is E[Wd] = d



−h3

> 32



= − ℓ6 

> 32

. The sum of variances is 

s2 

> d

= Pdi=1 Var (ξi,d ) = d

h3

> 16



+ dO(h4) → ℓ6 

> 16

. Finally, to establish asymptotic normality, we verify the Lyapunov condition for the fourth moment, 

> d

X

> i=1

E |ξi,d − Eξi,d |4 = O(dh 6) = O(d−1) → 0,

which completes the proof. Next, we establish the scaling limit of ESJD and the expected acceptance rate. 

Proposition F.3 (Optimal Scaling Limit) . As d → ∞ , the scaled ESJD converges to: 

lim  

> d→∞

Jd(ℓ) = ℓ2 · A(ℓ), A(ℓ) := E

 11 + exp( −W )



(24) 

where W ∼ N (−σ2/2, σ 2) and σ2 = ℓ6/16 .Proof. The proof follows the same line of argument as in Roberts and Rosenthal (1998); Agrawal et al. (2023). Observe that in stationarity, the d components are identically distributed. Thus, the full ESJD is exactly d times the single-coordinate ESJD: 

Jd(ℓ) = d−2/3 · d · E (˜ x1 − x1)2αd(x, ˜x) = d1/3E (˜ x1 − x1)2αd(x, ˜x) (25) Write the single-coordinate squared jump distance as ( ˜x1 − x1)2 = hz 21 + R1,d , where R1,d =

O(h3/2) almost surely. Multiplying by d1/3, we note d1/3h = ℓ2. Thus, d1/3( ˜x1 − x1)2 → ℓ2z21 almost surely. Let Wd, −1 = Pdi=2 ξi,d . Because ξ1,d 

> P

−→ 0, by Slutsky’s Theorem, Wd, −1

> D

−→ W . Furthermore, since Wd, −1 is strictly independent of z1, the joint distribution converges: (z1, W d, −1) D

−→ (z1, W ),where z1 ⊥⊥ W . By Slutsky’s Theorem again on the pair (z1, W d) = ( z1, W d, −1) + (0 , ξ 1,d ), we have 

(z1, W d) D

−→ (z1, W ).22 00.05 0.1 0.15 0.2 0.25 0.3 0.35 0.4 0.45 0.5  

> Expected Acceptance Rate
> 00.05 0.1 0.15 0.2 0.25 0.3 0.35 0.4 0.45
> Expected Squared Jumping Distance
> Efficiency vs. Acceptance Rate
> ESJD Curve
> Optimal Rate 0.347

Figure 4: Optimal acceptance rate for Algorithm 2 Let g(w) = (1 + exp (−w)) −1 represent Barker’s function. Because g is continuous and bounded, 

g(w) ∈ (0 , 1) , the function f (z, w ) = z2g(w) is continuous. By the Continuous Mapping Theorem: 

Vd := d1/3(˜ x1 − x1)2αd(x, ˜x) D

−→ ℓ2z21 g(W ). (26) To pass the limit inside the expectation, we require the sequence Vd to be uniformly integrable. Indeed, since 0 ≤ αd ≤ 1, we have |Vd| ≤ d1/3[h2 

> 4

x21 − h3/2x1z1 + hz 21 ] ≤ ℓ4 

> 4

x21 + ℓ3|x1z1| + ℓ2z21 .Because x1, z 1 ∼ N (0 , 1) , the dominating variable is integrable, which implies uniform integrability of {Vd}d≥1. It follows that: 

lim  

> d→∞

Jd(ℓ) = lim  

> d→∞

E[Vd] = E ℓ2z21 g(W ) = ℓ2E[z21 ]E[g(W )] = ℓ2E

 11 + exp( −W )



. (27) Finally, Theorem F.3 allows one to determine the optimal acceptance rate via numerical simulations. See Figure 4 for the efficiency curve, ℓ2 · A(ℓ) versus A(ℓ). By choosing the optimal ℓ,which corresponds to the optimal step size h that maximizes the ESJD, the expected acceptance rate converges to 0.347 (to three decimal places) as d → ∞ .

# G Experiment details 

Synthetic 2D Datasets In these experiments, we used the ancestral sampler from DDPM (Ho et al., 2020) as the predictor. This uses the discretisation 

xt = 1

p1 − βt+1 

(xt+1 + βt+1 sϕ(xt+1 , t + 1)) + pβt+1 zi+1 

where the forward noising process assumes a discrete Markov chain {x0, . . . , xT } constructed as 

p(xt+1 |xt) ∼ N (xt+1 ; √1 − βtxt, β tI) with 0 < β 1, . . . β T < 1. The diffusion sampling hyperparame-ters for these experiments are listed in Table 2. 23 Table 2: Diffusion sampling hyperparameters for experiments on synthetic 2D datasets. Dataset Predictor Steps MADM steps per Predictor Step Step Size h at pt

spiral 40 20 10 −1 × βt+1 

funnel 10 20 βt+1 

sierpinski triangle 20 30 10 −2 × βt+1 

pinwheel 20 30 10 −2 × βt+1 

Image Datasets All experiments were run on a single NVIDIA H100 GPU. In these experiments, we used the probability flow ODE as the predictor by using pre-trained models (config F) from (Karras et al., 2022). We used the same number of predictor steps (18 for CIFAR-10, 40 for FFHQ and AFHQv2, 256 for ImageNet). We use the default sampling strategy in their codebase, which uses the scale function s(t) = 1 and schedule σ(t) = t. This yields a simplified expression for the probability flow ODE (Equation 4 in (Karras et al., 2022)) 

dYt = σ(t)∇x log ptmax −t(Yt) d t. 

where the ODE is runs forward in time (i.e. t goes from 0 to tmax ). The diffusion sampling hyperparameters for these experiments are listed in Table 3. 

# H Licenses 

Codebases: 

• Elucidating the Design Space of Diffusion-Based Generative Models (Karras et al., 2022): Attribution-NonCommercial-ShareAlike 4.0 International. Datasets: 

• CIFAR-10 (Krizhevsky and Hinton, 2009): MIT license. 

• FFHQ (Karras et al., 2019): Creative Commons BY-NC-SA 4.0 license. 

• AFHQv2 (Choi et al., 2020): MIT license. 

• ImageNet (Deng et al., 2009): Unknown license. 24 Table 3: Diffusion sampling hyperparameters for experiments on image datasets. Run times are based on a batch size of 64. Dataset ODE Solver Predictor Steps MADM Steps MADM Step Size h at pt Run Time CIFAR-10 Heun 18 - - 3 secs CIFAR-10 Heun 18 1 10 −2 × σ(t) 3 secs CIFAR-10 Euler 18 - - 2 secs CIFAR-10 Euler 18 1 10 −2 × σ(t) 3 secs FFHQ Heun 40 - - 10 secs FFHQ Heun 40 1 10 −2 × σ(t) 24 secs FFHQ Euler 40 - - 8 secs FFHQ Euler 40 1 10 −2 × σ(t) 20 secs AFHQv2 Heun 40 - - 10 secs AFHQv2 Heun 40 1 10 −2 × σ(t) 24 secs AFHQv2 Euler 40 - - 8 secs AFHQv2 Euler 40 1 10 −3 × σ(t) 20 secs ImageNet Heun 256 - - 3 mins ImageNet Heun 256 1 10 −2 × σ(t) 4 mins ImageNet Euler 256 - - 2 mins ImageNet Euler 256 1 10 −2 × σ(t) 3 mins 25