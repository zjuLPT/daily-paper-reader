Title: TimeTok: Granularity-Controllable Time-Series Generation via Hierarchical Tokenization

URL Source: https://arxiv.org/pdf/2605.01418v1

Published Time: Tue, 05 May 2026 00:45:24 GMT

Number of Pages: 23

Markdown Content:
# TimeTok : Granularity-Controllable Time-Series Generation via Hierarchical Tokenization 

Seokhyun Lee * 1 Jaeho Kim * 1 Changjun Oh 1 Mihaela van der Schaar 2 Changhee Lee 1

## Abstract 

Time-series generative models often lack control over temporal granularity, forcing users to accept whatever granularity the model produces. To en-able truly user-driven generation, we introduce 

TimeTok , a unified framework for Granularity-Controllable Time-Series Generation (GC-TSG ), which generates time series at any target granular-ity from any coarser input ( e.g., rough sketches) or from scratch. At the core of TimeTok is a hierar-chical tokenization strategy that maps time series into an ordered sequence of tokens, from coarse to fine temporal granularity. Our autoregressive gen-eration process operates across these granularity levels, producing token blocks that are decoded back into continuous time series. This design natu-rally enables GC-TSG – including standard gener-ation – within a single framework, where control-ling the number of token blocks provides explicit control over output detail. Experiments show that 

TimeTok excels at GC-TSG tasks while achiev-ing state-of-the-art performance in standard gen-eration. Furthermore, we showcase TimeTok ’s potential as a foundational tokenizer by training on multiple datasets with heterogeneous tempo-ral granularities, verifying strong transferability that consistently outperforms models trained on individual datasets. To our knowledge, this is the first unified framework that covers the full gener-ative spectrum for time series, offering a valuable foundation for models that benefit from diverse temporal granularities. 

## 1. Introduction 

Time-series data are inherently multiscale; they can be ob-served, represented, and analyzed at different levels of tem-

> 1

Department of Artificial Intelligence, Korea University, Seoul, South Korea 2Department of Applied Mathematics and Theoretical Physics, University of Cambridge, Cambridge, United Kingdom. Correspondence to: Changhee Lee <changheelee@korea.ac.kr >.

Preprint. May 5, 2026. 

poral granularities. Fine-grained representations preserve rapid fluctuations and fine-scale dynamics, while coarse-grained representations emphasize slowly varying trends and long-range behaviors. This multiscale nature is par-ticularly evident in high-impact domains such as finance and healthcare, where decision-making and data analysis routinely span multiple temporal granularities. In finance, traders make decisions across diverse temporal granulari-ties, ranging from millisecond-level high-frequency trad-ing (Moreno-Pino & Zohren, 2024) to monthly portfolio re-balancing (Hwang et al., 2025). Similarly, in healthcare, car-diologists rely on hospital-grade electrocardiogram (ECG) signals sampled at up to 1000 Hz, whereas consumer-grade smartwatches collect ECG data at much lower sampling rates (often below 60 Hz) (Isakadze & Martin, 2020), poten-tially missing subtle but clinically relevant patterns. These examples highlight a common challenge: practition-ers must reason across granularities – using coarse obser-vations to interpret, anticipate, or reconstruct fine-grained temporal dynamics. Yet, acquiring fine-grained time-series data is often costly, invasive, or impractical, leading to sys-tematic mismatches between the granularity of available data and that required for downstream analysis. This gap motivates the development of time-series generation frame-works that operate flexibly across temporal scales. More specifically, an effective generative model should support synthesis at any target granularity , either from scratch or by refining coarse inputs such as low-fidelity observations or user-provided sketches. Such capability would allow explicit control over temporal detail, supporting truly user-driven and application-specific time-series generation. 

Contribution. In this work, we propose TimeTok , a novel unified framework for granularity-controllable time-series generation (GC-TSG ) that can synthesize time series at ar-bitrary temporal granularities and refine coarse inputs into fine-grained outputs. At the core of TimeTok is a hierar-chical tokenization strategy that represents a time series as an ordered sequence of discrete tokens. These tokens are structured to progress from coarse to fine temporal granular-ities, where each level incrementally refines the underlying temporal structure ( e.g., moving from long-range behaviors to fine-scale dynamics). Building on this representation, we introduce an autoregressive generation process that oper-1                     

> arXiv:2605.01418v1 [cs.AI] 2 May 2026 TimeTok : Granularity-Controllable Time-Series Generation 1Token 4Tokens 16 Tokens 32 Tokens 64 Tokens 128 Tokens Ground Truth
> ECG5000 Nasdaq
> 2Tokens 8Tokens
> Figure 1. Visualization at Different Token Lengths. TimeTok encodes time series into a hierarchically ordered sequence of discrete tokens that progressively capture coarse-to-fine temporal granularities. We visualize samples from the finance ( i.e., Nasdaq stock data) and medical domain ( i.e., ECG signals); additional results are provided in Section C.

ates over granularity levels. The generated token blocks are subsequently decoded using the learned tokenizer, allowing 

TimeTok to naturally support GC-TSG – including stan-dard time-series generation – within a single, unified frame-work. Extensive experiments demonstrate that TimeTok 

achieves state-of-the-art performance on both generation tasks, delivering high-fidelity and diverse samples while also improving prediction performance in downstream tasks. Finally, we demonstrate the scalability of TimeTok by extending it from an individual tokenizer trained on a sin-gle dataset to a foundational tokenizer trained on the UTSD dataset (Liu et al., 2024). By learning from multiple datasets with heterogeneous temporal granularities, the resulting foundational tokenizer exhibits strong transferability across diverse domains, consistently outperforming models trained on individual datasets. 

## 2. Related Works 

2.1. Time-Series Generation 

While deep generative models have demonstrated state-of-the-art results in various modalities, time-series generation presents unique difficulties due to complex temporal depen-dencies. TimeGAN (Yoon et al., 2019) is one of the earliest works that adapts generative adversarial networks (GANs) to time series, leveraging supervised forecasting signals to sta-bilize training and improve temporal coherence. TimeVQ-VAE (Lee et al., 2023) utilizes vector quantization (Van Den Oord et al., 2017) to build discrete (codebook) rep-resentations across time and frequency domains, followed by autoregressive generation using a bidirectional trans-former ( i.e., MaskGIT (Chang et al., 2022)). More recently, diffusion-based models such as Diffusion-TS (Yuan & Qiao, 2024) have achieved high-fidelity synthesis by directly pre-dicting the original time series during the denoising process. Despite their success, these methods are inherently designed to generate time series only at a fixed temporal granularity determined by the corresponding training data. Enabling granularity control within these works typically requires ad-ditional training with explicit conditioning mechanisms or architectural modifications. In contrast, TimeTok naturally supports GC-TSG as part of its training objective, allow-ing explicit and flexible control over temporal granularity through a hierarchical token representation. 

2.2. Multi-Granular Discrete Representation Learning Time Series. Recent advances in time-series tokenization have enabled the development of generalist models such as TOTEM (Talukder et al., 2024), as learning discrete repre-sentations helps mitigate the noise and high dimensionality inherent to temporal data. However, only a limited number of approaches have explicitly modeled temporal structure within the latent token space. WaveToken (Masserano et al., 2025) adopts a wavelet-based tokenizer that applies the discrete wavelet transforms to decompose signals into time-localized frequency components. TransPL (Kim & Lee, 2025) decomposes time-series segments ( i.e., patches) into trend and residual components, which are quantized using separate coarse and fine codebooks. While these methods in-corporate multi-granularity structures into time-series repre-sentations, the number of tokens is predetermined and does not adapt to the desired temporal granularity. Consequently, they do not provide explicit control over the granularity of the generated output, nor do they integrate tokenization into a generative process that can incorporate granularity levels. 

Computer Vision. Meanwhile, multi-granularity structures have been more deeply integrated into both tokenization and generation in computer vision. FlowAR (Ren et al.) proposes a scale-wise autoregressive framework augmented with flow matching, where images are first encoded into continuous latent representations using a VAE and then or-ganized into multi-scale token maps through progressive downsampling. These hierarchical tokens then guide a flow matching module to generate images from coarse to fine scales. FlexTok (Bachmann et al., 2025) encodes images with a VAE and resamples the resulting latent representa-tions into an ordered and content-dependent sequence of tokens via nested dropout. Despite these advances, hierarchi-cal tokenization remains largely unexplored in the context of time-series generation. In this paper, we explicitly define and formalize GC-TSG as 2TimeTok : Granularity-Controllable Time-Series Generation 

a novel task that requires flexible, user-controlled generation across temporal granularities. By translating hierarchical paradigms from computer vision to the temporal domain, we propose a hierarchical tokenization scheme in which token levels are structurally organized from coarse to fine temporal granularities. This design enables TimeTok to traverse granularity levels during generation, naturally supporting GC-TSG within a single, unified framework. 

## 3. Modeling Time-Series Generation Across Temporal Granularities 

In this work, we posit that a time-series generative model should not be bound to a single resolution but should instead treat granularity as a controllable dimension. 

3.1. Generation with Multiple Temporal Granularity 

Let x = [ x1, . . . , x T ] ∈ RT be a univariate time series of length T , drawn from an unknown data distribution pdata ,

i.e., x ∼ pdata (x). We formally define granularity level 

ℓ ∈ { 1, . . . , L } as a specification of how much temporal detail is preserved in a given time series. For each time series x, we denote x(ℓ) as a time series with temporal gran-ularity level ℓ where each x(ℓ) shares the same high-level semantics as x but differs in its temporal details; a smaller value corresponds to coarser representations, while a larger value preserves more fine-grained temporal information. By convention, we set x(L) def 

= x, corresponding to the finest granularity and indicating the original time series. 

Standard Time-series Generation. The Standard time-series generation focuses on learning a density pθ whose samples are indistinguishable from those drawn from the marginal data distribution pdata , achieving pθ (x) dist. 

= pdata (x),where dist. 

= denotes equality in distribution. While fundamen-tal, it cannot offer control over the temporal detail of outputs or conditioning on high-level temporal structures. 

Granularity-Controllable Time-series Generation. We define GC-TSG as a generalized time-series generation task with explicit control over temporal granularity for both con-ditioning and synthesis as follows: 

Definition 3.1 (GC-TSG ). Let x(i) and x(j) denote time-series representations at temporal granularity levels 

i, j ∈ { 0, 1, . . . , L } with j > i . We define granularity-controllable time-series generation ( GC-TSG ) as the task of learning a generative model pθ whose samples are indis-tinguishable from those drawn from the conditional data distribution across all valid pairs of temporal granularities, achieving pθ (x(j) | x(i)) dist. 

= pdata (x(j) | x(i)). Here, we slightly abuse notation by defining x(0) = ∅, which corre-sponds to the unconditioned setting. This formulation unifies unconditional generation ( i.e., i =0) and conditional refinement ( i.e., i ≥ 1) under a single framework. In particular, standard time-series generation is given as a special case by setting i = 0 and j = L.

3.2. Autoregressive Formulation of GC-TSG 

In practice, time-series datasets typically provide only the finest-grained observations x(L). To construct the multi-granular hierarchy required for granularity-controllable time-series generation in a principled manner, we intro-duce a family of information-reducing operators {R ℓ}ℓ∈[L].Each operator Rℓ maps the raw time series to its represen-tation at temporal granularity level ℓ, i.e., x(ℓ) = Rℓ(x).Here, smaller values of ℓ induce stronger information reduc-tion, resulting in coarser abstractions of the time series. For instance, Rℓ can be implemented as a Gaussian smoothing kernel (Zhao & Wei, 2003) or a moving-average operator, where ℓ controls the smoothing variance or the window size, respectively, to determine the temporal granularity 1.A core objective of GC-TSG is to model conditional distri-butions over time-series representations for any valid pair of temporal granularities, using only the finest-grained time series ( i.e., observations). To achieve this, we decompose the marginal log-likelihood of the observed time series by applying the chain rule along the granularity axis: 

log pθ (x) = log pθ

 x(≤L) =

> L

X

> ℓ=1

log pθ

 x(ℓ)| x(<ℓ ) (1) where x(<ℓ ) =  x(1) , . . . , x(ℓ−1) . The first equality holds because each coarse-granular representation x(ℓ) is deter-ministically obtained from x via Rℓ. This hierarchical au-toregressive decomposition induces a joint distribution over representations at all temporal granularities and, in doing so, defines a valid refinement process that incrementally introduces finer temporal details. Under this formulation, generating x(j) conditioned on x(i)

is realized by sampling a coarse-to-fine sub-trajectory. Con-ditioning on a prefix consistent with x(i), ancestral sam-pling proceeds through successive conditional distributions 

pθ

 x(ℓ)| x(<ℓ ) for ℓ = i + 1 , . . . , j , progressively refining the time-series representations until reaching granularity level j. This procedure ensures that the generated x(j) re-mains both statistically consistent with the conditioning input x(i) and coherent with the hierarchical structure. Unfortunately, learning and sampling from this hierarchi-cal formulation in Eq. (1) present two key challenges: (i) Implementing each refinement step – i.e., pθ (x(ℓ)|x(<ℓ )) –directly in the raw time-series space is inherently difficult, as it requires conditioning on the entire trajectory of preced-

> 1In practice, the degree of temporal granularity can be quan-tified using established measures in time series, such as the de-trended fluctuation analysis (DFA) exponent (Peng et al., 1995).

3TimeTok : Granularity-Controllable Time-Series Generation Encoder  

> (B) Autoregressive Modeling in Token Space
> Time Series x
> Level
> Measurement
> Decoder
> Encoded Tokens
> …Encoder
> Keep Tokens at Level
> …
> VAR
> Transformer

❄

❄  

> … Generated Tokens
> …
> (A) Input Tokenization (B) Token Generation (C) Decoding
> “Level 1”
> “Level 2”
> “Level 8”
> … …

❄   

> Quantizer
> Time Series x
> Register Tokens 𝐑
> Quantizer Patched TS Emb 𝐄

🔥  

> 1…
> …
> …
> Flow -Matching Decoder
> (A) Hierarchical Tokenization

🔥  

> smoothing
> VAR Transformer

🔥                    

> …
> (1) Training
> (2) Inference
> P!(")P!($)|!(")
> …
> 2128
> 1121…2128 BOS
> 1
> 1
> 234
> P!(%)|!(&$)
> 1…264
> …128
> P!(')|!(&()
> 12
> 65 66
> “Level 3” Prompt Tokens
> ℛℓ
> 𝐱 (%)𝐱 (')𝐱 (()
> Quantized Tokens 𝐐

Figure 2. An Overview of TimeTok . (1) Training: The hierarchical tokenizer is trained to encode input time series into a sequence of discrete tokens structured in a coarse-to-fine temporal hierarchy. These tokens are then used to train the VAR Transformer that autoregressively predicts tokens at the next granularity level. (2) Inference: A conditioning time series of unknown granularity is encoded into a full token sequence, from which tokens at the inferred granularity are selected as a prefix for the VAR Transformer to generate the remaining tokens. The completed token sequence is then decoded into a time series at the target granularity. 

ing coarser signals, which makes training computationally expensive and often unstable, especially for long time series. (ii) Conditional generation given x(i) implicitly assumes access to the full coarse-level prefix x(≤i), which is typi-cally unavailable at inference time and otherwise needs to be reconstructed or approximated. These challenges motivate hierarchical modeling in a structured discrete token space. 

## 4. Method: TimeTok 

To address the challenge of modeling and generating time series with hierarchical dependencies across temporal gran-ularities, we introduce TimeTok , a novel framework for GC-TSG comprising three key components; an overview of 

TimeTok is illustrated in Figure 2: • Hierarchical Tokenization. Continuous time series are mapped into a sequence of discrete tokens that encode temporal structure in a coarse-to-fine hierarchy. • Autoregressive Modeling in Token Space. Generative modeling is then performed in the learned discrete token space using a hierarchical autoregressive model. • Granularity-Controllable Inference. At inference time, coarse time-series structure is encoded into a token pre-fix, and additional tokens are autoregressively generated up to a target granularity. The resulting token sequence is subsequently decoded to produce a time-series realiza-tion at the desired temporal granularity. 

4.1. Hierarchical Tokenization for Granularity Control 

To encode a coarse-to-fine temporal hierarchy directly in the discrete latent space, we introduce a structured tokeniza-tion in which the token ordering is semantically aligned with temporal granularity. Specifically, the leading tokens are constrained to represent high-level information of the time series, while successive tokens incrementally encode fine-scale temporal dynamics. In contrast to conventional tokenization schemes that require generating a fixed-length token sequence irrespective of the target granularity, our approach allows variable-length token sequences depending on the temporal granularity. 

Discrete Representation via Register Tokens. Follow-ing the discrete representation learning paradigm (Van Den Oord et al., 2017; Mentzer et al., 2023), we assume an encoder–decoder architecture (Enc , Dec ) and a quantizer 

Q. Given an input time series x, we first partition it into 

W non-overlapping patches of equal length. Each patch is linearly projected into a latent space, producing a sequence of patch embeddings E = [ e1, . . . , eW ] ∈ RW ×h, where 

h denotes the hidden dimension. To obtain a compact and structured representation, we append M learnable register tokens R = [ r1, . . . , rM ] ∈ RM ×h (Darcet et al., 2024) to the patch embeddings, forming the augmented sequence 

E ⊕ R ∈ R(W +M )×h, where ⊕ denotes concatenation. This sequence is then processed by a Transformer-based encoder ( Enc ). We denote the encoder outputs correspond-ing to the register tokens as eR = [˜ r1, . . . , ˜rM ]. Each en-coded register token is subsequently discretized by finite scalar quantization (FSQ) (Mentzer et al., 2023), result-ing in a sequence of discrete latent codes. In our imple-mentation, we configure the FSQ codebook with levels 

[4,4,4,4,4,4] , which maps each register token to a 6-dimensional discrete code, inducing an effective vocab-4TimeTok : Granularity-Controllable Time-Series Generation 

ulary size of 46. The resulting quantized register tokens 

Q = [ q1, . . . , qM ] = [ Q(˜ r1), . . . , Q(˜ rM )] ∈ RM ×6 are used as the conditioning input to the decoder. 

Token Conditioned Flow Matching Decoder. To en-force the desired hierarchical structure, we design the de-coder ( Dec ) using a Conditional Flow Matching frame-work (Lipman et al., 2023). Flow Matching generates samples by continuously transforming a source distribu-tion ( e.g., Gaussian noise) into the data distribution via a time-dependent velocity field. Our key idea is to endow the quantized register tokens with explicit hierarchical se-mantics by training the decoder to reconstruct time-series representations at different temporal granularities, depend-ing on how many tokens are provided. For each target granularity level ℓ, we define nℓ as the num-ber of tokens required to represent the signal at that level, with nL = M . In practice, we set nℓ = 2 ℓ−1 and fix the maximum granularity level to L = 8 . Empirically, we observe that uniform and exponential token allocations produce nearly identical losses across levels, indicating that fewer tokens suffice for coarser levels (Appendix Sec-tion B.2). The decoder is conditioned only on the nℓ leading tokens Q1: nℓ = [ q1, . . . , qnℓ ] and tasked with generating the corresponding time-series representation x(ℓ). Here, the target x(ℓ) is obtained from the original signal x via the information-reducing operator Rℓ, i.e., x(ℓ) = Rℓ(x).2 In contrast to conventional decoders, which condition the ve-locity field on the full token sequence Q, we train the veloc-ity predictor vθ (parameterized by a neural network) under varying conditioning budgets, corresponding to different numbers of available tokens. Each conditioning budget in-duces a distinct generative trajectory: conditioning on fewer tokens encourages the model to capture coarse, high-level temporal structure, while additional tokens progressively refine generation with finer details. Formally, for each granularity level ℓ, we define the linear interpolation between a clean sample x(ℓ) and noise ϵ ∼N (0, I ) at time τ ∈ [0 , 1] as 

x(ℓ) 

> τ

= (1 − τ ) ϵ + τ x(ℓ), (2) with the corresponding target velocity v(ℓ) 

> τ

= x(ℓ) − ϵ. This velocity specifies the instantaneous direction that transports noise toward x(ℓ), and is approximated by the parameterized velocity vθ conditioned on Q1: nℓ . We train vθ using the following coarse-to-fine flow-matching objective: 

LCTF (θ) = Ex, ϵ, τ, ℓ 

h

vθ (x(ℓ) 

> τ

| Q1: nℓ , τ ) − v(ℓ)

> τ
> 22

i

, (3) where the granularity level ℓ is sampled uniformly during training. This objective explicitly enforces that conditioning   

> 2In our implementation, we instantiate Rℓas a Gaussian smoothing operator that progressively removes fine-grained tem-poral details. See more details provided in Appendix A.1.

on only the first nℓ tokens suffices to reconstruct the time-series representation with granularity level ℓ, thus inducing a coarse-to-fine semantic ordering over the token sequence. 

4.2. GC-TSG through Autoregressive Transformer 

Once the encoder and decoder (Enc , Dec ) are trained under the objective in Eq. (3) , we freeze these components and shift the generative task to the learned hierarchical token space. This allows us to reformulate the data log-likelihood, 

log pθ (x), in terms of the corresponding tokens using a block-wise autoregressive factorization: 

log pϕ

 Q1: nL

 =

> L

X

> ℓ=1

log pϕ

 Qnℓ−1+1: nℓ Q1: nℓ−1

, (4) where the conditional distributions are parameterized by ϕ

and implemented using a VAR Transformer (Tian et al., 2024). Unlike conventional autoregressive models that generate sequences via token-by-token, next-token prediction, our VAR Transformer operates at the level of granularity blocks. It predicts a group of tokens corresponding to the next tem-poral granularity level, conditioned on all preceding coarser-level tokens as a prefix. We train pϕ using the following block-wise autoregressive objective: 

L(ϕ) = −EQ

" LX

> ℓ=1

log pϕ

 Qnℓ−1+1: nℓ Q1: nℓ−1 )

#

, (5) where Q is sampled from the tokenized time series in the training set. By sequentially generating token blocks across granularity levels, the model learns to refine coarse time-series representations with progressively finer details. This sequential block generation realizes Eq. (1) in the token space, capturing the full refinement trajectory with only M

tokens – i.e., the number of tokens required to represent a finest-grained time series – that enable efficient GC-TSG. 

4.3. Granularity-Controllable Generation (Inference) 

The key capability of TimeTok is its ability to generate fine-grained time series from coarse inputs, such as low-fidelity signals or user-provided sketches. Given a source granularity level i and a target level j, TimeTok performs GC-TSG through the following three steps: 

> 1

⃝ Input Tokenization. Since the input granularity is not known a priori, we first encode the input time series into a sequence of tokens Q. We then determine its granularity level i by evaluating the reconstruction error at each level (detailed in Section A.2). The prefix Q1: ni ,corresponding to the inferred level i, is then extracted to serve as the initial conditioning for the VAR Transformer. 5TimeTok : Granularity-Controllable Time-Series Generation 

Table 1. Standard Generation Performance. Generation performance on classification (ECG5000, ItalyPowerDemand) and forecast-ing (Nasdaq, ETTh1) tasks. We report accuracy (ACC) and F1 for classification, MSE for forecasting, and FID for both. Method ECG5000 ItalyPowerDemand Nasdaq ETTh1 FID ↓ ACC ↑ F1 ↑ FID ↓ ACC ↑ F1 ↑ FID ↓ MSE ↓ FID ↓ MSE ↓

Oracle – 0.959 ±0.001 0.620 ±0.010 – 0.982 ±0.008 0.982 ±0.008 – 0.033 ±0.000 – 0.098 ±0.000 

RNN(TF) 0.220 ±0.047 0.895 ±0.014 0.383 ±0.024 0.125 ±0.034 0.903 ±0.027 0.903 ±0.027 0.462 ±0.083 0.046 ±0.000 1.195 ±0.040 0.104 ±0.000 

RNN(PF) 0.076 ±0.028 0.884 ±0.013 0.398 ±0.035 0.101 ±0.019 0.952 ±0.020 0.952 ±0.021 1.070 ±0.119 0.054 ±0.001 0.730 ±0.015 0.112 ±0.001 

TimeGAN 0.034 ±0.002 0.815 ±0.042 0.348 ±0.018 0.026 ±0.008 0.958 ±0.003 0.958 ±0.003 0.316 ±0.050 0.061 ±0.001 1.068 ±0.243 0.104 ±0.001 

TimeVQVAE 0.033 ±0.011 0.925 ±0.004 0.380 ±0.001 0.060 ±0.011 0.979 ±0.003 0.979 ±0.003 0.136 ±0.015 0.048 ±0.001 0.107 ±0.009 0.102 ±0.001 

DiffusionTS 0.041 ±0.009 0.896 ±0.015 0.552 ±0.028 0.023 ±0.005 0.979 ±0.003 0.979 ±0.003 0.208 ±0.015 0.056 ±0.001 0.083 ±0.011 0.101 ±0.001 

TOTEM 0.022 ±0.006 0.923 ±0.004 0.379 ±0.001 0.016 ±0.004 0.979 ±0.007 0.979 ±0.007 0.164 ±0.008 0.046 ±0.001 0.102 ±0.015 0.102 ±0.001 

TimeTok 0.004 ±0.001 0.948 ±0.006 0.564 ±0.047 0.007 ±0.001 0.982 ±0.000 0.982 ±0.000 0.091 ±0.017 0.045 ±0.001 0.070 ±0.006 0.103 ±0.000 

> 2

⃝ Token Generation. Using Q1: ni as the initial context, we generate tokens in a coarse-to-fine manner. For each successive level ℓ = i+1 , . . . , j , we sample the corre-sponding token block: 

ˆQnℓ−1+1: nℓ ∼ pϕ

  · Q1: ni , ˆQni+1: nℓ−1

,

conditioning on both the original prefix and all previously generated tokens (with ˆQni+1: nℓ−1 = ∅ for ℓ = i+1 ). This iterative process continues until we obtain the com-plete token sequence with granularity level j.

> 3

⃝ Decoding. The resulting token sequence is decoded back to the time-series representation at granularity level j,

i.e., ˆx(j) = DEC  Q1: ni ⊕ ˆQni+1: nj

.Generation from scratch is a special case in which no coarse input is provided. In this setting, the conditioning context re-duces to the [BOS] token, from which we autoregressively generate nj tokens. Decoding the resulting tokens gives a time series at granularity level j. Similarly, generating all 

nL tokens becomes standard time-series generation. In both conditional and unconditional settings, class conditioning can be incorporated via AdaLN (Peebles & Xie, 2023). 

## 5. Experiments 

We provide the details of the datasets and experiments. Full implementation details, evaluation metrics, and ablation study can be found in Appendix A and B.2, respectively. 

Datasets & Generation Tasks. We evaluate TimeTok on two classification datasets (ECG5000 and ItalyPowerDe-mand) and two forecasting datasets (Nasdaq and ETTh1). For standard generation, where time series are generated from scratch ( [BOS] token), we measure the quality and distributional similarity between the synthetic and real data. In GC-TSG , we generate ˆx(j) from x(i) (j > i > 0), and evaluate (i) the diversity of the generated sequences ˆx(j)

and its alignment with x(j), and (ii) structural consistency between x(i) and ˆx(j).

Evaluation Metrics. In standard generation, we evaluate whether the generated time series highly aligns with real TimeTok (Ours) TOTEM DiffusionTS     

> TimeVQVAE TimeGAN RNN (PF )
> Generated Real

Figure 3. t-SNE visualization of ECG5000 . We observe a higher overlap between the synthetic and real data distribution for TimeTok , while other methods show signs of mode collapse. 

data. To assess this alignment, we measure (i) predictive score (Yoon et al., 2019) – i.e., the downstream classifica-tion or forecasting performance in a Train-Synthetic-Test-Real (TSTR) setup – and (ii) FID score (Yuan & Qiao, 2024) – i.e., distributional similarity between synthetic and real data. For GC-TSG , we evaluate the ability to generate diverse fine-grained signals ˆx(j) from coarse inputs x(j),while preserving the global structure of the coarse signals. As such, we use CRPS sum (Continuous Ranked Probabil-ity Score) (Fan et al., 2024), a commonly used metric for probabilistic forecasting, to measure diversity and consis-tency. We also measure the C-Cons (coarse consistency) between x(i) and re-coarsened ˆx(j) using RMSE. 

Baseline Methods. We compare TimeTok against state-of-the-art methods including RNNs trained with teacher forcing (TF) (Sutskever et al., 2011), professor forc-ing (PF) (Lamb et al., 2016), TimeGAN (Yoon et al., 2019), TimeVQVAE (Lee et al., 2023), DiffusionTS (Yuan & Qiao, 2024), and TOTEM (Talukder et al., 2024). 

Extension to Foundational Tokenizer. So far, we have assumed that the granularity level of a given time-series dataset corresponds to the finest granularity level ( i.e., 

x(L) def 

= x). This assumption, however, may not hold in heterogeneous settings, such as foundation model training, where data naturally spans multiple temporal granularities 6TimeTok : Granularity-Controllable Time-Series Generation 

Table 2. Conditional Generation. Conditional GC-TSG evaluation under two settings: Coarse-to-Fine ( i = 1 , j = 8 ) and All-Pairs average (Avg, i → j, 0 < i < j ). C-Cons denotes coarse-level consistency, measured by RMSE. Full results of TimeTok in Section D.                                                                                     

> Classification (ECG5000) Forecasting (Nasdaq) C2F ( 1→8)C2F-All Pairs ( i→j)C2F ( 1→8)C2F-All Pairs ( i→j)Method CRPS sum ↓C-Cons ↓CRPS sum ↓C-Cons ↓CRPS sum ↓C-Cons ↓CRPS sum ↓C-Cons ↓
> RNN(TF) 4.326 ±0.612 0.037 ±0.011 5.602 ±1.869 0.059 ±0.020 38.76 ±16.37 0.256 ±0.176 86.72 ±52.09 0.483 ±0.295
> RNN(PF) 3.640 ±0.800 0.025 ±0.010 3.797 ±1.067 0.034 ±0.011 50.40 ±26.94 0.347 ±0.213 48.20 ±22.14 0.370 ±0.215
> TimeGAN 3.155 ±0.626 0.011 ±0.003 1.737 ±0.737 0.011 ±0.005 38.73 ±21.46 0.183 ±0.104 39.11 ±22.14 0.196 ±0.105
> TimeVQVAE 4.144 ±0.702 0.041 ±0.012 3.693 ±0.858 0.044 ±0.013 35.16 ±31.10 0.269 ±0.242 35.32 ±34.30 0.282 ±0.258
> DiffusionTS 3.273 ±1.588 0.028 ±0.017 2.672 ±1.551 0.031 ±0.019 101.9 ±64.64 0.752 ±0.533 101.5 ±63.26 0.771 ±0.538
> TOTEM 3.208 ±1.042 0.033 ±0.017 2.639 ±1.286 0.035 ±0.019 36.81 ±20.18 0.269 ±0.234 35.97 ±22.54 0.294 ±0.247
> TimeTok 3.042 ±0.900 0.011 ±0.007 1.587 ±0.867 0.010 ±0.006 17.72 ±12.63 0.089 ±0.085 15.87 ±12.48 0.096 ±0.090

Table 3. Marginal Generation. Generation to each granularity level on Nasdaq, evaluated by FID. Due to limited space, we list L1, L4, and L8 and provide the full results in Section B. 

Method L1 ↓ L4 ↓ L8 ↓

RNN(TF) 8.029 ±0.629 8.173 ±0.654 3.362 ±0.317 

RNN(PF) 4.522 ±0.380 3.755 ±0.292 4.421 ±0.384 

TimeGAN 9.043 ±0.344 9.528 ±0.982 9.833 ±0.781 

TimeVQVAE 1.935 ±0.157 1.169 ±0.037 0.645 ±0.035 

DiffusionTS 0.198 ±0.019 0.369 ±0.028 0.739 ±0.063 

TOTEM 0.748 ±0.038 0.722 ±0.057 1.058 ±0.085 

TimeTok 0.088 ±0.010 0.090 ±0.016 0.091 ±0.017 

and uniformly treating all time series as finest-granular may be inappropriate. To address this gap, we extend TimeTok 

to a foundational setting by training the hierarchical tok-enizer on UTSD (Liu et al., 2024), a multi-domain time-series collection designed for foundation models. We evalu-ate the transferability of TimeTok under this setup; more details are provided in Section 5.3. 

5.1. Standard Time-Series Generation Quantitative Performance. For the TSTR setup, we train a backbone model – i.e., InceptionTime (Ismail Fawaz et al., 2020) for classification, DLinear (Zeng et al., 2023) for fore-casting – and report its predictive performance. We provide class conditioning to all models used in classification. As shown in Table 1, TimeTok achieves the best performance on ECG5000, ItalyPowerDemand, and Nasdaq, and com-petitive performance on ETTh1, in both predictive and FID metrics. Here, Oracle indicates the predictive performance when the prediction models are trained on the real data. 

Visualizations with t-SNE. Figure 3 shows t-SNE visual-izations of the ECG5000 dataset. Synthetic data generated by TimeTok (red) strongly overlaps with the real data dis-tribution (blue), indicating high diversity and fidelity. In contrast, baseline methods show evidence of mode collapse or incomplete coverage of the true distribution. 

5.2. Granularity-Controlled Time-Series Generation 

We evaluate TimeTok on GC-TSG with two related tasks: (i) conditional generation, where given a coarse input, each model generates diverse and finer-grained time series at a specified target granularity while preserving the coarse struc-ture, and (ii) unconditional generation, where each model generates time series at a specified target granularity level from scratch. We provide the results in Table 2 and Table 6, respectively. For fair comparison, we adapt the baselines to support granularity control. Since these methods cannot inherently generate time series at a specified granularity, we train them on datasets containing paired coarse- and fine-grained representations, allowing conditional genera-tion from a coarse input to a specified target granularity. For unconditional generation, we replace the coarse input with a zero or Null embedding. See details in Section A.5. 

Conditional Generation. We evaluate conditional gener-ation using two metrics (details in Section A). First, we generate five time series for each coarse input, and measure the CRPS sum (lower is better) to evaluate the diversity and its preservation of global structure. TimeTok consistently outperforms all baselines in both datasets. Second, we mea-sure coarse-level consistency (C-Cons) by re-coarsening the generated time series and computing the RMSE against the original coarse input. TimeTok again achieves the best performance across all baselines. 

Unconditional Generation. Using the [BOS] token, 

TimeTok generates time series at granularity levels 1 through 8, corresponding to 1, 2, 4, 8, 16, 32, 64, and 128 tokens, respectively; level 8 corresponds to standard generation. Table 6 shows that TimeTok achieves the best FID (lower is better) across all levels compared to baselines. Notably, TimeTok maintains stable performance across all levels (FID: 0.088–0.121), whereas the second-best model, TOTEM, exhibits much larger variance (FID: 0.580–1.058). From both conditional and marginal generation results, we observe that baseline methods such as DiffusionTS and TimeGAN struggle to jointly capture conditional and 7TimeTok : Granularity-Controllable Time-Series Generation 

Table 4. Foundation tokenizer evaluation on ECG5000 and Nasdaq datasets. Metrics: FID (lower is better), ACC, F1, and MSE.                                                  

> ECG5000 Nasdaq Method FID ↓ACC ↑F1 ↑FID ↓MSE ↓
> TimeVQVAE 0.367 0.924 0.450 0.082 0.047 TOTEM 0.035 0.929 0.394 0.068 0.045
> TimeTok (w/o DFA) 0.042 0.944 0.583 0.111 0.044
> TimeTok (w/ DFA) 0.010 0.951 0.668 0.046 0.042 Prompt: Generate asimplified and coarse time series
> sequence that represents abull market ...(more)
> <LLM Generated Sketch> <TimeTok Output>

Figure 4. User-provided Sketch . We visualize fine-grained time series generated from coarse sketches provided by the user. 

marginal distributions. In contrast, TimeTok performs well on both, which we attribute to TimeTok ’s direct learning of Eq. (1) in the hierarchical token space. 

5.3. Extension to Foundational Time-series Tokenization 

In the foundational setting, we train TimeTok on UTSD (Liu et al., 2024). We apply the same Rℓ’s used in the individual setup, but determine the absolute granularity level for each time series using the DFA scaling exponent, which is discretized into levels via uniform binning. Based on these estimated granularities, we first train the hierarchical tokenizer on UTSD and then train the VAR transformer on downstream tasks using this foundational tokenizer. In Table 4, we evaluate the standard generation perfor-mance of our foundational tokenizer using two variants of TimeTok : (i) w/o DFA, using the individual setup, and (ii) w/ DFA, using the DFA-based granularity levels. For fair comparison, we also train TimeVQVAE and TOTEM on UTSD. Both variants outperform TimeVQVAE, with the DFA-based variant showing a substantial performance gain. We attribute this improvement to training on a large, heterogeneous dataset with diverse temporal granularities, which helps the tokenizer better capture granularity-specific structures. These results underscore TimeTok ’s promise as a foundational tokenizer for time series. 

5.4. Qualitative Analysis User-provided Coarse Sketch. We generate a coarse input via an LLM ( e.g., “an increasing stock trend” in Figure 4) and use TimeTok to transform this coarse sketch into a fine-grained, realistic time series. See full results in Section F. 

Clustering Analysis by Granularity. We perform a clus-Level 1 Clusters TimeTok : Generated 100 Samples at Level 8

Figure 5. Centroid Visualization . We first identify the tokens used in Level 1 centroids, and use this token as a condition to generate Level 8 samples. We visualize the centroids of Level 8. 

tering analysis of the generated time series based on their granularity level (Figure 5). As illustrated in the figure, the model generates diverse samples enriched with fine-grained details, while preserving the coarse trend in Level 1. 

Token-usage Analysis. We analyze the token-usage pattern of TimeTok on Nasdaq in Section G. 

5.5. Additional Analysis 

Due to limited space, additional results are provided in the appendix, with key highlights summarized here. 

GC-TSG Visualization. We visualize the coarse-to-fine refinement process in GC-TSG (Section D). 

Information-Reducing Operators. We evaluate the robust-ness of our method using a test set constructed with various information-reducing operators (Section E). 

Ablation. We conduct ablation studies on key design com-ponents of TimeTok to assess their impact and explore alternative design choices (Section B.2). 

## 6. Conclusion 

In this work, we proposed TimeTok , a novel time-series generation model capable of generating time series at any target granularities, whether conditioned on coarse-grained inputs or generated entirely from scratch. This flexibility is made possible with our proposed hierarchical tokenization strategy, where simply controlling the number of tokens for decoding controls the granularity level of the output – enabling truly user-driven generation of time series. We envision TimeTok as a promising generative model that can enable the acquisition of diverse multi-granularity data needed for training time-series foundation models, while also solving a wide range of practical, real-world problems. 8TimeTok : Granularity-Controllable Time-Series Generation 

## Impact Statement 

This paper presents a time-series generation model with applications in healthcare, finance, and other scientific do-mains. Synthetic data generation can enhance privacy and security, but it may also be misused to generate misleading or fraudulent data. We encourage responsible use of the generated outputs. 

## References 

Bachmann, R., Allardice, J., Mizrahi, D., Fini, E., Kar, O. F., Amirloo, E., El-Nouby, A., Zamir, A., and De-hghan, A. Flextok: Resampling images into 1d token se-quences of flexible length. In Forty-second International Conference on Machine Learning , 2025. URL https: //openreview.net/forum?id=DgdOkUUBzf .Bishop, C. M. Mixture density networks. 1994. Bishop, C. M. and Nasrabadi, N. M. Pattern recognition and machine learning , volume 4. Springer, 2006. Chang, H., Zhang, H., Jiang, L., Liu, C., and Freeman, W. T. Maskgit: Masked generative image transformer. In 

Proceedings of the IEEE/CVF conference on computer vision and pattern recognition , pp. 11315–11325, 2022. Darcet, T., Oquab, M., Mairal, J., and Bojanowski, P. Vi-sion transformers need registers. In The Twelfth In-ternational Conference on Learning Representations ,2024. URL https://openreview.net/forum? id=2dnO3LLiJ1 .Fan, X., Wu, Y., Xu, C., Huang, Y., Liu, W., and Bian, J. Mg-tsd: Multi-granularity time series diffusion models with guided learning process. In The Twelfth International Conference on Learning Representations , 2024. Hwang, Y., Lee, Y., Lee, J., Zohren, S., Kim, J. H., Kim, W. C., Lee, Y., and Fabozzi, F. J. Deep learning in as-set management: Architectures, applications, and chal-lenges. Applications, and Challenges (September 15, 2025) , 2025. Isakadze, N. and Martin, S. S. How useful is the smartwatch ecg? Trends in cardiovascular medicine , 30(7):442–448, 2020. Ismail Fawaz, H., Lucas, B., Forestier, G., Pelletier, C., Schmidt, D. F., Weber, J., Webb, G. I., Idoumghar, L., Muller, P.-A., and Petitjean, F. Inceptiontime: Finding alexnet for time series classification. Data Mining and Knowledge Discovery , 34(6):1936–1962, 2020. Kim, J. and Lee, S. Transpl: Vq-code transition matrices for pseudo-labeling of time series unsupervised domain adaptation. In Forty-second International Conference on Machine Learning , 2025. Lamb, A. M., ALIAS PARTH GOYAL, A. G., Zhang, Y., Zhang, S., Courville, A. C., and Bengio, Y. Professor forcing: A new algorithm for training recurrent networks. 

Advances in neural information processing systems , 29, 2016. Lee, D., Malacarne, S., and Aune, E. Vector quantized time series generation with a bidirectional prior model, 2023. URL https://arxiv.org/abs/2303.04743 .Lipman, Y., Chen, R. T. Q., Ben-Hamu, H., Nickel, M., and Le, M. Flow matching for generative modeling. In The Eleventh International Conference on Learning Represen-tations , 2023. URL https://openreview.net/ forum?id=PqvMRDCJT9t .Liu, Y., Zhang, H., Li, C., Huang, X., Wang, J., and Long, M. Timer: Generative pre-trained transformers are large time series models. arXiv preprint arXiv:2402.02368 ,2024. Masserano, L., Ansari, A. F., Han, B., Zhang, X., Faloutsos, C., Mahoney, M. W., Wilson, A. G., Park, Y., Rangapu-ram, S. S., Maddix, D. C., et al. Enhancing foundation models for time series forecasting via wavelet-based tok-enization. In Forty-second International Conference on Machine Learning , 2025. Mentzer, F., Minnen, D., Agustsson, E., and Tschannen, M. Finite scalar quantization: Vq-vae made simple. arXiv preprint arXiv:2309.15505 , 2023. Moreno-Pino, F. and Zohren, S. Deepvol: Volatility fore-casting from high-frequency data with dilated causal con-volutions. Quantitative Finance , 24(8):1105–1127, 2024. Peebles, W. and Xie, S. Scalable diffusion models with transformers. In Proceedings of the IEEE/CVF interna-tional conference on computer vision , pp. 4195–4205, 2023. Peng, C.-K., Havlin, S., Stanley, H. E., and Goldberger, A. L. Quantification of scaling exponents and crossover phenomena in nonstationary heartbeat time series. Chaos: an interdisciplinary journal of nonlinear science , 5(1): 82–87, 1995. Ren, S., Yu, Q., He, J., Shen, X., Yuille, A., and Chen, L.-C. Flowar: Scale-wise autoregressive image genera-tion meets flow matching. In Forty-second International Conference on Machine Learning .Sutskever, I., Martens, J., and Hinton, G. E. Generating text with recurrent neural networks. In Proceedings of the 28th international conference on machine learning (ICML-11) , pp. 1017–1024, 2011. 9TimeTok : Granularity-Controllable Time-Series Generation 

Talukder, S. J., Yue, Y., and Gkioxari, G. Totem: Tokenized time series embeddings for general time series analysis. 

Transactions on Machine Learning Research , 2024. Tian, K., Jiang, Y., Yuan, Z., PENG, B., and Wang, L. Vi-sual autoregressive modeling: Scalable image generation via next-scale prediction. In The Thirty-eighth Annual Conference on Neural Information Processing Systems ,2024. URL https://openreview.net/forum? id=gojL67CfS8 .Van Den Oord, A., Vinyals, O., et al. Neural discrete rep-resentation learning. Advances in neural information processing systems , 30, 2017. Yoon, J., Jarrett, D., and Van der Schaar, M. Time-series generative adversarial networks. Advances in neural in-formation processing systems , 32, 2019. Yuan, X. and Qiao, Y. Diffusion-ts: Interpretable diffu-sion for general time series generation. In The Twelfth International Conference on Learning Representations ,2024. Zeng, A., Chen, M., Zhang, L., and Xu, Q. Are transformers effective for time series forecasting? In Proceedings of the AAAI conference on artificial intelligence , volume 37, pp. 11121–11128, 2023. Zhao, S. and Wei, G.-W. Jump process for the trend esti-mation of time series. Computational Statistics & Data Analysis , 42(1-2):219–241, 2003. 10 TimeTok : Granularity-Controllable Time-Series Generation 

## A. EXPERIMENT DETAILS        

> Figure 6. Example of Gaussian kernel smoothing with varying ℓvalues. The original signal ( Original ) is shown together with smoothed signals for different ℓ. Note that ℓ= 8 is identical to the original signal.

A.1. Gaussian kernel smoothing 

Target signal x(ℓ) is constructed by smoothing the original sequence x(L) def 

= x with a Gaussian kernel, x(ℓ) = gσ(ℓ) ∗ x,where gσ(ℓ) denotes a discrete normalized Gaussian kernel, i.e., P 

> i

gσ [i] = 1 . Specifically, the kernel weights are given by 

gσ(ℓ)[i] ∝ exp 



− i2

> 2·σ(ℓ)2



and truncated to the window |i| ≤ 3 · σ(ℓ). The bandwidth σ(ℓ) is defined as a function of the level ℓ:

σ(ℓ) = σmax ·

 L − ℓL − 1



where we set σmax = T / 12 in practice, which results in an effective maximum kernel support of approximately 6 · σmax =

T / 2 due to truncation at |i| ≤ 3 · σ(ℓ). This design assigns stronger smoothing to smaller ℓ, yielding coarse representations dominated by long-range structure, while gradually reducing the smoothing as ℓ increases so that higher levels progressively recover fine-grained details. Since Gaussian kernel smoothing is not uniquely invertible, recovering fine-scale details requires learning progressive refinement across higher granularity levels. 

A.2. Level Measurement 

Given a time series with unknown granularity, TimeTok must identify the appropriate granularity level to perform GC-TSG .While users can manually specify the level based on prior knowledge, such information may not always be available. To address this, we implement an automatic level measurement based on the bias-variance tradeoff principle (Bishop & Nasrabadi, 2006): 

> 1

⃝ Time-Series Tokenization. We tokenize the input time series at the finest granularity level, retaining all tokens. 

> 2

⃝ Distance Measurement. We decode the tokens at each granularity level ℓ ∈ { 1, . . . , 8} obtaining time-series at each granularity level. We then compute the L2 distance between the input time series and each reconstructed time series. 

> 3

⃝ Complexity-Simplicity Tradeoff. Our objective is to balance token efficiency with reconstruction distance. Rather than simply selecting the level with the minimum distance, we identify the level that yields the highest relative improve-ment ( i.e., the steepest decrease in distance relative to the previous level). This approach selects a level that captures the coarse information while avoiding unnecessary complexity. The use of our level-measurement function was a design choice, where other measurement functions ( e.g., Knee-detection) can be used for level detection. 11 TimeTok : Granularity-Controllable Time-Series Generation 

A.3. DATASETS   

> Table 5. Dataset Statistics. For each dataset, we report the number of samples in the original train/test splits, the total number of samples, the number of classes, and the length of the time series. For the UTSD dataset, we report the actual number of samples used to train
> TimeTok . Here, we randomly sample 1% of the original data due to computational budget.

ID Dataset Name # Train # Test # Total # Classes Input Len Pred Len TS Len 

1 ECG5000 500 4500 5000 5 – – 140 2 ItalyPowerDemand 67 1029 1096 2 – – 24 3 Nasdaq 5320 1520 6840 – 196 48 244 4 ETTh1 4900 1400 6300 – 96 48 144 5 UTSD 2,954,239 - 2,954,239 – - - 256 

A.4. Evaluation Metric CRPS: For conditional GC-TSG , our goal is to evaluate how accurately the model captures the conditional distribution 

p(x(j) | x(i)), where x(i) denotes coarse time series and x(j) the corresponding target sequence. Given a condition x(i), the model generates K samples {ˆx(j,k )}Kk=1 (with K=5 in all experiments) intended to represent the conditional predictive distribution of x(j). We evaluate the conditional predictive distribution pointwise in time using the Continuous Ranked Probability Score (CRPS), a strictly proper scoring rule for univariate predictive distributions. At each time step t, CRPS is estimated from the generated samples {ˆx(j,k ) 

> t

}Kk=1 and the ground-truth target value x(j) 

> t

as 

\CRPS( Ft, x (j) 

> t

) = 1

K

> K

X

> k=1

|ˆx(j,k ) 

> t

− x(j) 

> t

| − 12K2

> K

X

> k=1
> K

X

> k′=1

|ˆx(j,k ) 

> t

− ˆx(j,k ′) 

> t

|.

To assess the quality of the entire conditional trajectory, we aggregate the pointwise scores over the temporal horizon and define 

CRPS sum =

> T

X

> t=1

\CRPS( Ft, x (j) 

> t

).

Following prior work on time-series evaluation, we report CRPS sum averaged over the test set (Fan et al., 2024). A lower CRPS sum indicates that the generated samples more accurately represent the conditional distribution p(x(j) | x(i)), both in terms of accuracy and uncertainty calibration. 

Coarse consistency: In addition to CRPS, we measure coarse consistency , which evaluates whether generated target sequences remain consistent with the provided coarse condition. Recall that information-reducing operator is defined as 

x(i) = Ri(x(L)), i.e., Ri is explicitly specified as a mapping from the finest-grained observations x(L) to x(i). Therefore, 

Ri is not directly applicable to an arbitrary intermediate-level signal x(j). To define a consistency metric for a generated sample ˆx(j), we instead require an operator that maps level j to level i (with i < j ), which we denote by Rj→i. We then recoarsify each generated target sample via Rj→i(ˆ x(j)) and compute the RMSE against the ground-truth condition x(i):

C-cons = RMSE 



Rj→i(ˆ x(j)), x(i)

,

averaged over generated samples and test pairs. This metric is well-defined when R is instantiated as Gaussian smoothing. In this case, a level is parameterized by the Gaussian standard deviation, and Gaussian filters are closed under composition: applying Gaussians with σ1 and σ2 sequentially is equivalent to a single Gaussian with pσ21 + σ22 . Hence, given the known smoothing parameters for levels i and j, we can construct Rj→i by applying an additional Gaussian whose variance matches the difference, i.e., σ2 

> j→i

= σ2 

> i

− σ2 

> j

. For other information-reducing operators, an unambiguous mapping Rj→i is generally unavailable, so C-cons is reported only for Gaussian smoothing and omitted otherwise. 

A.5. Baseline Implementation for GC-TSG 

Directly realizing Eq. (1) in the raw time-series space is fundamentally challenging. Each refinement step – i.e., 

pθ (x(ℓ)|x(<ℓ )) – requires the model to condition on the full trajectory of all preceding coarser time-series representa-12 TimeTok : Granularity-Controllable Time-Series Generation 

tions, which makes training computationally expensive and often unstable for long time series. Common workarounds – such as modeling pθ (x(j)|x(i)) for all granularity pairs – fail to impose an explicit hierarchical ordering over temporal granularities, which may struggle to enforce consistent coarse-to-fine refinement or to capture dependencies across distant temporal granularities. To empirically substantiate these considerations, we present a comparative study of baseline implementations that realize GC-TSG in different representational spaces. Among them, the AR Transformer directly realizes Eq. (1) in the raw time-series space by modeling the full coarse-to-fine refinement trajectory, whereas the remaining baselines approximate GC-TSG by modeling the pairwise conditional pθ (x(j) | x(i)) across granularity pairs. Specifically, we examine three variants that differ in where the hierarchical refinement is instantiated. A detailed comparison with the AR Transformer is provided in Table 9 for conditional generation and Table 10 for unconditional (marginal) generation. • GC-TSG in raw time-series space. RNN-TF/PF. We implement RNN-TF and RNN-PF baselines using an encoder-decoder architecture with Gated Recurrent Units (GRUs) and a Mixture Density Network (MDN) (Bishop, 1994) head. The models map the target granularity level ℓ ∈ { 1, . . . , 8} to a learnable embedding vector eℓ, which is injected into the decoder as a global condition. To incorporate the coarse condition c, the encoder summarizes the sequence to initialize the decoder’s hidden state. Additionally, at each time step t, the decoder receives a concatenated input vector consisting of the previous output xt−1, the level embedding eℓ, and the local condition value ct. The MDN head maps the decoder’s hidden states to the parameters of a Gaussian Mixture Model for probabilistic generation. RNN-TF is trained via teacher forcing to minimize the negative log-likelihood, while RNN-PF employs adversarial training with a Bi-directional GRU discriminator to align hidden state dynamics and mitigate exposure bias. For marginal generation, c is replaced with a zero-valued sequence. 

Diffusion-TS. We extend the Diffusion-TS by incorporating the target granularity level and a coarse-condition. The model encodes the target granularity level ℓ ∈ { 1, . . . , 8} into a vector embedding eℓ using an MLP, which is injected into the Transformer backbone via Adaptive Layer Normalization alongside timestep embeddings. To incorporate the coarse condition c, we concatenate it with the noisy input along the feature dimension. The model is trained to reconstruct the target time series at level ℓ from the noisy input, conditioned on eℓ and c. For marginal generation, conditioned on eℓ alone, we replace c with a learnable Null embedding and train to reconstruct the target time series at level ℓ.

AR Transformer. We train an autoregressive Transformer with the MDN head on the concatenated 8-level sequence 

s = x(1) ⊕ . . . ⊕ x(8) ∈ R8T . The model uses learnable positional embeddings. To indicate level transitions, we insert a learnable [SEP] token at each level boundary with corresponding level embedding eℓ added to [SEP] (and [BOS] for level 1). For unconditional generation, we begin from [BOS] and generate sequentially up to the desired target level. For conditional generation, given a condition at level i, the model requires the full trajectory up to that level i

(i.e., x(1) , . . . , x(i)) as context. Unlike TimeTok , where coarser levels are naturally obtained during tokenization, the AR Transformer cannot derive them from a single-level condition. Therefore, we provide the ground-truth trajectory 

{x(ℓ)}iℓ=1 as a prefix and generate levels i+1 through the target level autoregressively. • GC-TSG in continuous latent space. TimeGAN. We extend the TimeGAN framework to support granularity-controllable generation. The generator receives 

ϵ ⊕ c ⊕ eℓ as input, where ϵ is random noise, c is the coarse condition sequence, and eℓ is the target level embedding. Unlike standard GANs, the generator produces a sequence of embeddings in the latent space, which is then mapped to the time-series space via a recovery network. The discriminator also operates in the latent space, distinguishing real latent representations h (derived from real time series via an embedding network) from generated ones ˆh using the concatenated input h ⊕ c ⊕ eℓ. To enable marginal generation, we omit the coarse condition ( i.e., c = 0) for unconditional training samples, allowing the model to generate samples conditioned solely on the target granularity level. 13 TimeTok : Granularity-Controllable Time-Series Generation 

• GC-TSG in discrete token space. TimeVQVAE. We adapt the two-stage TimeVQVAE framework for granularity-controllable generation. In Stage 1, we train the VQ-VAE tokenizer on time series at all granularity levels ℓ ∈ { 1, . . . , 8} by randomly sampling x(ℓ)

during training, enabling the tokenizer to learn a unified discrete representation across varying granularities. In Stage 2, we extend the MaskGIT (Chang et al., 2022) by concatenating condition tokens, a [SEP] token, and masked target tokens. To encode the target granularity level, we add a learnable level embedding eℓ to the [SEP] token. For marginal generation, we replace the condition tokens with a learnable Null embedding and generate conditioned on [SEP] +eℓ

alone. For conditional generation, we tokenize the condition time series and generate conditioned on both the condition prefix and [SEP] +eℓ.

TOTEM. We adapt the two-stage TOTEM framework for granularity-controllable generation. In Stage 1, we train a VQ-VAE tokenizer that directly processes raw time series across all granularity levels ℓ ∈ { 1, . . . , 8}, enabling the model to learn a unified discrete representation for both high-resolution targets and coarsified signals. In Stage 2, we implement an Autoregressive Transformer to handle the generative process. The transformer receives a prefix consisting of the tokens from the source condition time series and a [SEP] token, where the target level information is integrated into the [SEP] token via a learnable level embedding eℓ. During training, the model is optimized using Teacher Forcing to predict the subsequent target tokens sequentially in an autoregressive manner. For marginal generation, the source condition sequence is replaced by a zero-valued input, and the model generates the target time series based on the [SEP] token and its associated level information alone. 14 TimeTok : Granularity-Controllable Time-Series Generation 

## B. ADDITIONAL EXPERIMENTAL RESULTS 

In this section, we present additional experiments that we omitted in the main body of the paper due to limited space. 

B.1. Full Results 

Table 6. Marginal (Unconditional) Generation Performance of Nasdaq. FID is evaluated at each granularity level (L1–L8). 

Method L1 ↓ L2 ↓ L3 ↓ L4 ↓ L5 ↓ L6 ↓ L7 ↓ L8 ↓

RNN(TF) 8.029 ±0.629 7.490 ±0.606 8.406 ±1.007 8.173 ±0.654 8.859 ±0.154 11.34 ±1.167 11.05 ±2.312 3.362 ±0.317 

RNN(PF) 4.522 ±0.380 4.047 ±0.594 4.496 ±0.243 3.755 ±0.292 4.009 ±0.195 4.209 ±0.440 4.132 ±0.278 4.421 ±0.384 

TimeGAN 9.043 ±0.344 9.956 ±0.738 9.436 ±0.563 9.528 ±0.982 9.386 ±0.419 9.115 ±0.546 10.247 ±0.891 9.833 ±0.781 

TimeVQVAE 1.935 ±0.157 0.596 ±0.035 1.009 ±0.095 1.169 ±0.037 0.853 ±0.056 0.686 ±0.040 1.420 ±0.117 0.645 ±0.035 

DiffusionTS 0.198 ±0.019 0.252 ±0.019 0.322 ±0.040 0.369 ±0.028 0.539 ±0.035 0.440 ±0.077 0.597 ±0.103 0.739 ±0.063 

TOTEM 0.748 ±0.038 0.631 ±0.058 0.589 ±0.031 0.722 ±0.057 0.671 ±0.057 0.795 ±0.045 0.580 ±0.022 1.058 ±0.085 

TimeTok 0.088 ±0.010 0.100 ±0.010 0.121 ±0.027 0.090 ±0.016 0.106 ±0.013 0.130 ±0.014 0.094 ±0.007 0.091 ±0.017 

Table 7. Marginal (Unconditional) Generation Performance of ECG5000. FID is evaluated at each granularity level (L1–L8). 

Method L1 ↓ L2 ↓ L3 ↓ L4 ↓ L5 ↓ L6 ↓ L7 ↓ L8 ↓

RNN(TF) 2.778 ±1.096 1.393 ±0.500 0.459 ±0.129 1.121 ±0.430 0.643 ±0.150 0.206 ±0.064 0.286 ±0.101 0.188 ±0.057 

RNN(PF) 0.649 ±0.106 0.485 ±0.079 0.202 ±0.036 0.170 ±0.042 0.118 ±0.044 0.105 ±0.026 0.199 ±0.023 0.082 ±0.021 

TimeGAN 0.139 ±0.024 0.224 ±0.036 0.263 ±0.093 0.477 ±0.143 0.989 ±0.186 0.872 ±0.358 1.474 ±0.414 1.870 ±0.296 

TimeVQVAE 0.045 ±0.011 0.031 ±0.005 0.017 ±0.002 0.017 ±0.004 0.031 ±0.003 0.021 ±0.004 0.036 ±0.009 0.034 ±0.011 

DiffusionTS 0.017 ±0.004 0.018 ±0.002 0.022 ±0.006 0.019 ±0.006 0.025 ±0.006 0.022 ±0.008 0.018 ±0.003 0.023 ±0.004 

TOTEM 0.025 ±0.005 0.020 ±0.001 0.024 ±0.005 0.015 ±0.004 0.028 ±0.010 0.022 ±0.005 0.039 ±0.009 0.058 ±0.016 

TimeTok 0.001 ±0.000 0.001 ±0.001 0.001 ±0.000 0.001 ±0.000 0.002 ±0.001 0.003 ±0.001 0.003 ±0.001 0.006 ±0.001 

15 TimeTok : Granularity-Controllable Time-Series Generation 

Table 8. Full Results of GC-TSG . Full results for granularity-controlled time-series generation ( GC-TSG ) on the ECG5000 and Nasdaq datasets. Here, i denotes the source level and j the target level, i.e., generating level-j time series from level-i input. We report CRPS sum 

and coarse-consistency (C-Cons), where C-Cons measures the RMSE between the original coarse input and the generated output after re-coarsening with a Gaussian filter. 

Classification (ECG5000) Forecasting (Nasdaq) 

i → j CRPS sum ↓ C-Cons ↓ CRPS sum ↓ C-Cons ↓

1 → 2 1.079 ±0.408 0.011 ±0.004 20.09 ±16.54 0.091 ±0.073 

1 → 3 1.037 ±0.364 0.009 ±0.004 15.18 ±10.77 0.076 ±0.061 

1 → 4 1.374 ±0.568 0.010 ±0.007 15.02 ±12.54 0.081 ±0.079 

1 → 5 1.718 ±0.526 0.010 ±0.006 14.50 ±11.52 0.083 ±0.078 

1 → 6 2.173 ±0.674 0.011 ±0.007 16.48 ±12.47 0.095 ±0.088 

1 → 7 2.730 ±0.848 0.011 ±0.006 17.11 ±12.47 0.096 ±0.088 

1 → 8 3.042 ±0.900 0.011 ±0.007 17.72 ±12.63 0.089 ±0.085 

2 → 3 0.969 ±0.354 0.010 ±0.004 14.55 ±9.16 0.075 ±0.056 

2 → 4 1.109 ±0.369 0.010 ±0.004 13.73 ±9.15 0.081 ±0.072 

2 → 5 1.485 ±0.556 0.010 ±0.006 14.57 ±10.87 0.090 ±0.082 

2 → 6 1.897 ±0.711 0.011 ±0.006 15.29 ±11.02 0.093 ±0.084 

2 → 7 2.401 ±0.924 0.011 ±0.006 15.58 ±11.03 0.088 ±0.082 

2 → 8 2.693 ±1.001 0.012 ±0.007 17.73 ±11.26 0.093 ±0.080 

3 → 4 1.022 ±0.589 0.011 ±0.006 13.94 ±9.85 0.090 ±0.078 

3 → 5 1.283 ±0.639 0.011 ±0.006 13.56 ±9.00 0.087 ±0.075 

3 → 6 1.637 ±0.776 0.012 ±0.007 13.93 ±10.06 0.084 ±0.079 

3 → 7 2.026 ±0.766 0.012 ±0.007 16.46 ±11.22 0.101 ±0.086 

3 → 8 2.322 ±0.874 0.012 ±0.007 17.82 ±12.09 0.102 ±0.089 

4 → 5 0.988 ±0.360 0.010 ±0.005 13.44 ±9.77 0.094 ±0.085 

4 → 6 1.265 ±0.468 0.011 ±0.006 14.38 ±10.80 0.096 ±0.087 

4 → 7 1.602 ±0.591 0.012 ±0.007 15.81 ±12.10 0.105 ±0.095 

4 → 8 1.858 ±0.638 0.011 ±0.006 17.67 ±14.28 0.105 ±0.101 

5 → 6 0.906 ±0.331 0.009 ±0.005 14.78 ±12.08 0.106 ±0.098 

5 → 7 1.195 ±0.396 0.009 ±0.005 15.34 ±12.95 0.104 ±0.101 

5 → 8 1.526 ±0.528 0.010 ±0.006 18.06 ±15.97 0.114 ±0.112 

6 → 7 0.914 ±0.390 0.007 ±0.005 16.34 ±16.50 0.121 ±0.123 

6 → 8 1.332 ±0.502 0.008 ±0.005 17.81 ±16.46 0.120 ±0.119 

7 → 8 0.835 ±0.378 0.006 ±0.005 17.53 ±16.12 0.129 ±0.126 

B.2. Ablation Study Token Allocation Across Granularity Levels. We study how the conditioning budget affects optimization by comparing our default exponential allocation nℓ = 2 ℓ−1 against a uniform allocation nℓ = 16 · ℓ, while fixing the maximum granularity level to L = 8 and the total finest-level budget to nL = M = 128 . As shown in Figure 7, the two token allocation strategies exhibit nearly identical training dynamics across all levels. Consistently, validation reconstruction losses on the original time-series data – measured as the mean squared error between generated sequences and the ground truth –follow similar trajectories under both allocations, as seen in Figure 8. These results suggest that coarse time-series representations require substantially fewer tokens to achieve comparable optimization and reconstruction behavior, making exponential token allocation an efficient design choice under a fixed budget. 

Comparison with AR Transformer in Raw Time-Series Space. We compare our approach with an AR Transformer that directly operates in the raw time-series space and explicitly models the full coarse-to-fine refinement trajectory according to Eq. (1) . Unlike our method, which approximates refinement via conditional generation across granularity pairs in a structured token space, the AR Transformer must condition on the entire refinement history, leading to unstable learning. The results reveal a clear contrast between conditional and marginal generation behaviors of the AR Transformer. As shown in Table 9, the AR Transformer demonstrates reasonable performance in the conditional setting, where external conditions provide sufficient guidance to partially stabilize the generation process. However, as evidenced in Table 10, its performance degrades substantially in the marginal (unconditional) generation setting. This failure highlights the intrinsic difficulty of learning and sampling the entire coarse-to-fine refinement trajectory in the raw time-series space. 16 TimeTok : Granularity-Controllable Time-Series Generation 

Figure 7. Level-wise training loss comparison between exponential (Pow2) and uniform (Equal Bin) token allocation strategies across all eight granularity levels. Both strategies exhibit nearly identical optimization behavior at every level, indicating that allocating fewer tokens to coarser levels does not adversely affect training dynamics. The x-axis denotes training progress, where one unit corresponds to 10 epochs. 

Figure 8. Validation reconstruction loss measured on the original time-series signals, comparing exponential (Pow2) and uniform (Equal Bin) token allocation strategies. The two approaches show nearly identical convergence trends, suggesting comparable reconstruction performance on the validation set. 

Table 9. Conditional Generation. Conditional GC-TSG evaluation under two settings: Coarse-to-Fine ( i = 1 , j = 8 ) and All-Pairs average (Avg, i → j, 0 < i < j ). C-Cons denotes coarse-level consistency, measured by RMSE. 

Classification (ECG5000) Forecasting (Nasdaq) C2F ( 1 → 8) C2F-All Pairs ( i → j) C2F ( 1 → 8) C2F-All Pairs ( i → j)Method CRPS sum ↓ C-Cons ↓ CRPS sum ↓ C-Cons ↓ CRPS sum ↓ C-Cons ↓ CRPS sum ↓ C-Cons ↓

AR Transformer 4.827 ±1.768 0.027 ±0.014 5.804 ±1.658 0.040 ±0.018 32.57 ±25.82 0.160 ±0.136 22.98 ±18.95 0.119 ±0.107 

TimeTok 3.042 ±0.900 0.011 ±0.007 1.587 ±0.867 0.010 ±0.006 17.72 ±12.63 0.089 ±0.085 15.87 ±12.48 0.096 ±0.090 

Table 10. Unconditional (marginal) generation performance across granularity levels (L1–L8), evaluated on ECG5000 and Nasdaq. 

Dataset Method L1 ↓ L2 ↓ L3 ↓ L4 ↓ L5 ↓ L6 ↓ L7 ↓ L8 ↓

ECG5000 AR Transformer 1.060 ±0.358 1.713 ±0.431 1.420 ±0.469 2.019 ±0.693 2.918 ±0.804 3.213 ±0.684 3.494 ±0.736 3.938 ±0.921 

Nasdaq AR Transformer 14.990 ±0.730 12.427 ±0.886 13.305 ±1.111 13.287 ±0.868 12.870 ±0.852 13.309 ±0.622 13.443 ±1.722 12.405 ±1.176 

17 TimeTok : Granularity-Controllable Time-Series Generation 

## C. Coarse To Fine Reconstruction 

We compare the coarse-to-fine reconstruction performance between individual tokenizers (I) trained on specific datasets and a foundation tokenizer (F) trained on the UTSD dataset, which contains time series from various domains. 1 Token 2 Tokens 16 Tokens 32 Tokens 64 Tokens 128 Tokens Ground Truth 

(I) 

(F)    

> 4Tokens 8Tokens

(I) 

(F) 

(I) 

(F) 

(I) 

(F) 

(I) 

(F) 

Figure 9. ECG5000 Reconstruction. 

18 TimeTok : Granularity-Controllable Time-Series Generation 1 Token 2 Tokens 16 Tokens 32 Tokens 64 Tokens 128 Tokens Ground Truth 

(I) 

(F) 

4 Tokens 8 Tokens 

(I) 

(F) 

(I) 

(F) 

(I) 

(F) 

(I) 

(F) 

Figure 10. Nasdaq Reconstruction. 

19 TimeTok : Granularity-Controllable Time-Series Generation 

## D. GC-TSG Generation 

Figure 11. GC-TSG of ECG5000. We visualize the generation of i → j in ECG5000 dataset. Our level detection module first detects the granularity level of the sample. Using this sample as the condition, we generate time series at target granularity. 

20 TimeTok : Granularity-Controllable Time-Series Generation 

## E. Other Information Reducing Operations 1.5                      

> 1.0
> 0.5
> 0.0
> 0.5
> 1.0
> 1.5
> Wavelet
> Level 1 Level 2 Level 3 Level 4 Level 5 Level 6 Level 7 Level 8
> 1.5
> 1.0
> 0.5
> 0.0
> 0.5
> 1.0
> 1.5
> Moving Avg
> Level 1 Level 2 Level 3 Level 4 Level 5 Level 6 Level 7 Level 8
> 1.5
> 1.0
> 0.5
> 0.0
> 0.5
> 1.0
> 1.5
> Downsample  Level 1 Level 2 Level 3 Level 4 Level 5 Level 6 Level 7 Level 8

Figure 12. Information Reducing Operators. Visualization of alternative information reducing operators for each granularity level 

Table 11. Evaluation Results of Other Information Reducing Operators. We report the CRPS sum value at GC-TSG task. 

ECG5000, C2F i → j Nasdaq, C2F i → j

Operator TimeGAN TimeVQVAE TimeTok TimeGAN TimeVQVAE TimeTok 

Wavelet 3.084 ±1.111 6.475 ±0.952 2.354 ±0.916 39.80 ±22.00 35.68 ±34.24 17.29 ±13.05 

Moving Average 2.702 ±1.199 3.884 ±0.852 2.080 ±0.905 39.66 ±21.99 35.75 ±34.99 17.13 ±12.91 

Downsample 2.686 ±1.126 3.496 ±0.998 2.126 ±0.915 39.51 ±22.15 34.63 ±34.14 16.50 ±12.55 

Gaussian 1.737 ±0.737 3.693 ±0.858 1.587 ±0.867 39.11 ±22.14 35.32 ±34.30 15.87 ±12.48 

In Table 11, we evaluate the GC-TSG performance of TimeTok on evaluation sets constructed with various information reducing operation functions ( Rℓ). Although TimeTok is trained exclusively with Gaussian kernel smoothing, it generalizes robustly to other operators such as wavelet transforms, moving averages, and down sampling. This robustness is particularly important for practical applications, as it demonstrates that TimeTok can effectively handle diverse coarse inputs conditions, such as user provided sketches and coarse outline. 21 TimeTok : Granularity-Controllable Time-Series Generation 

## F. Use Case Analysis Flat Market Bear Market Bull Market                                                                                                  

> Generate asimplified and
> coarse time series sequence
> that represents aflat market.
> The yaxis range from -1to 1.
> The sequence length is 244.
> Generate asimplified and
> coarse time series sequence
> that represents abear market.
> The yaxis range from -1to 1.
> The time series initially has
> adecreasing trend, but has a
> sudden increase, but overall
> gains back its momentum to
> decreasing back. The sequence
> length is 244.
> Generate asimplified and
> coarse time series sequence
> that represents abull market.
> The yaxis range from -1to 1.
> The time series initially has
> an increasing trend, but has
> asudden drop, but overall
> gains back its momentum to
> increasing back. The sequence
> length is 244.

Prompt 

LLM / User 

Provided 

Sketch 

TimeTok 

(# Case 1) 

TimeTok 

(# Case 2) 

Figure 13. Generating Fine-Grained Signals Conditioned on Coarse Outline: An Analysis in Financial Market Scenario. For this analysis, we used TimeTok trained on the Nasdaq dataset. We employed ChatGPT to write code that generates the coarse signal, then used TimeTok to generate time series at various granularity levels, conditioned on the given coarse signal. 

Complex time series are inherently difficult to describe using natural language due to their temporal structure and inherent randomness. In contrast, specifying a coarse outline of the time series is relatively straightforward: a user can simply draw the outline or ask an LLM to generate such signals or scenarios. This motivates an important application of TimeTok :generating fine-grained signals conditioned on a coarse outline. For instance, a financial analyst may want to model a bull market scenario, but with a slight drop in between. Such coarse outline can be easily provided with an LLM as shown in the bull market scenario in Figure 13. Using this coarse outline, the analyst can use TimeTok to generate realistic financial bear market movement at various granularity levels, conditioned on the signal. We note that such generation is grounded on the actual distribution of the stock market ( i.e., TimeTok ’s training data), differentiating with methods that simply add random noise to the coarse structure. 22 TimeTok : Granularity-Controllable Time-Series Generation 

## G. Token Usage Analysis on the Nasdaq Dataset High Volatility Medium Volatility Low Volatility 

# (A) 

# (B) (C)        

> Figure 14. Token Usage Pattern Analysis in Nasdaq. (A) We quantify samples based on their volatility, and visualize three representative samples. (B) To narrow down the analysis space, we filter to samples that start with token index 3260 , which generally exhibit an increasing trend pattern. From these samples, we visualize a heatmap depicting the token usage pattern at the full sequence length ( i.e.,
> 128 tokens). We observe that higher-volatility samples tend to utilize fewer tokens, whereas lower-volatility samples exhibit larger and more diverse token usage. (C) Understanding that such pattern occurs in the selected samples, we subsequently expand our focus to all samples. In the scatter plot, the x-axis represents the volatility, and the y-axis is the token usage entropy. We observe that such token-usage pattern occurs throughout the whole samples.

Our analysis focus on how the tokens in TimeTok are used to represent the given time series. To analyze token usage pattern in TimeTok , we used the Nasdaq synthetic dataset generated for the marginal experiment (the dataset used in Table 6). Specifically, we used samples generated at level 8 ( ℓ = 8 ), as they utilize the full tokens ( i.e., M = 128 ). We describe our analysis procedure in three stages. 

(A). Given the time series, we first quantify the volatility of each data samples by measuring the Turning Point Rate (), measuring the jaggedness of the data. In Figure 14-(A), we visualize samples based on the quantified measure. High volatility samples have high jaggedness, while low volatility samples have low jaggedness. 

(B). We then tokenize the time series. To focus our token analysis, we identify token index 3260 as the most frequently occurring first token ( ℓ = 1 ) in the Nasdaq dataset and retain only the samples that begin with this token. Since the first token captures the coarsest pattern of the time series, we observe that token index 3260 generally corresponds to an increasing trend. From these samples, we visualize the used token indexes from all token sequences ( i.e., 128) using a heatmap. We observe that higher-volatility samples tend to use smaller subset of tokens, while lower-volatility samples have more diverse use of tokens. 

(C). With this finding, we subsequently expand our analysis to the full sample space. We quantify the token usage pattern with token entropy, where a higher entropy value means more diverse use of tokens, and lower entropy corresponds to more selective use of tokens. From the scatter plot, we observe that a similar token usage pattern occurs throughout all samples. In summary, we observe that samples with high volatility use a smaller subset of tokens, while lower volatility samples use a larger subset of tokens. 23